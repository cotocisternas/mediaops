---
epic: 2
date: 09-02-2026
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 2 retrospective — Seedbox answers on this box

## Epic summary

- **Epic:** 2 — Seedbox answers on this box (`_bmad-output/planning-artifacts/epics.md` § Epic 2, stories 2.1–2.3).
- **Diff range:** `d2d9331^..3d64fb0` (`8130f81..3d64fb0`). First story commit is `d2d93318` (stand-up); last commit in the epic window is `3d64fb0` (sprint-row close). The left endpoint is the parent of the first commit so the stand-up is in the diff.
- **Stories completed (sprint-status, all `done`):** `2-1-net-crate-tls-identity-and-channels`, `2-2-mediaopsd-seedbox-role`, `2-3-seedbox-bootstrap-over-ssh`.
- **`pending_stories`:** `[]` (`detect-epic --epic 2`). Machine verdict is not forced to rejected.
- **Mode:** sprint. Headless. Phase 3 skipped.

### Commits (`git_evidence.py`)

`uv run --no-cache .agents/skills/bmad-retrospective/scripts/git_evidence.py --repo /home/coto/work/dev/mediaops --range d2d9331^..3d64fb0 --stories 2-1,2-2,2-3`

| sha | subject | is_merge | stories |
| --- | --- | --- | --- |
| `3d64fb0` | Close the Epic 2 sprint rows and the 1.2 review ledger. | false | `[]` |
| `6e2be4c` | Merge pull request #4 from cotocisternas/epic/2-seedbox-answers | true | `[]` |
| `b76cc23` | Close Epic 2 review findings for pool, walker, and bootstrap. | false | `[]` |
| `d2d9331` | Stand up seedbox mTLS, Transfer, and offline bootstrap for Epic 2. | false | `[]` |

`commit_count: 4`, `merge_count: 1`, `merges_measured: 1`. `stories` is empty on every commit: subjects name “Epic 2” / PR #4, not `2-1` / `2-2` / `2-3`. Per-story attribution therefore does not run. `binary_revisions` is 0 on every path. `files` is non-merge churn only; `merge_files` restates the first-parent merge of `6e2be4c` and is not added into `files`.

Highest non-merge change volume (text; `files`):

| path | added | deleted | net | commits |
| --- | ---: | ---: | ---: | ---: |
| `Cargo.lock` | 898 | 13 | 885 | 2 |
| `bins/mediaops/src/bootstrap.rs` | 699 | 72 | 627 | 2 |
| `crates/net/src/lib.rs` | 606 | 26 | 580 | 2 |
| `crates/ssh/src/lib.rs` | 474 | 15 | 459 | 2 |
| `crates/net/src/mint.rs` | 281 | 22 | 259 | 2 |
| `crates/store/src/lib.rs` | 262 | 36 | 226 | 2 |
| `crates/net/src/seedbox.rs` | 230 | 20 | 210 | 2 |
| `crates/core/src/walker.rs` | 194 | 6 | 188 | 2 |
| `crates/core/src/desired_state.rs` | 193 | 0 | 193 | 1 |
| `crates/net/src/listen.rs` | 178 | 27 | 151 | 2 |

`3d64fb0` touches only implementation-artifacts (`sprint-status.yaml`, `deferred-work.md`, 1.2/1.3 specs, `epic-1-context.md`). Production code last landed in `b76cc23`.

### Evidence inventory

| Item | Status |
| --- | --- |
| Epic spec + declared ACs | Present: `_bmad-output/planning-artifacts/epics.md` (Epic 2 summary ~L184–186; stories 2.1–2.3 ~L325–400). Companion: SPEC-mediaops `stories.yaml` ids 5–7; Architecture Spine AD-5, AD-7, AD-12, AD-14, AD-16, AD-21, AD-22. |
| Story files under `{implementation_artifacts}` | **Missing.** Epic 1 has `spec-1-*`. Epic 2 has no `spec-2-*`. Intent is the epic file + `stories.yaml`. |
| Diff range + `git_evidence.py` | Present. Range `d2d9331^..3d64fb0`. |
| Sprint status | `_bmad-output/implementation-artifacts/sprint-status.yaml`: epic-2 `done`, three stories `done`, `epic-2-retrospective: optional`. No `action_items` key. |
| Review artifact | `_bmad-output/implementation-artifacts/review-pr-4-epic-2.md` — all listed items `[x]` (patch, decision, or defer). Re-checked against HEAD before routing. |
| Previous retrospective | **None.** No `epic-1-retro-*.md` (or any `*retro*`) under implementation-artifacts. |
| Session logs | **Absent.** No conversation/session records for 2.1–2.3. Process-lesson analysis from logs is skipped. |
| `project-context.md` | **Absent** (`workflow.persistent_facts` glob `{project-root}/**/project-context.md`). |
| Live SeedIt4Me bootstrap | **Not exercised** in this run and not present in the epic commits (no `live.rs` at `b76cc23`). |

## Findings

Each item has a source and a disposition (`fix now` / `defer` / `accept`). Sub-agent reports were not used; lenses ran inline. Review-PR findings already patched in `b76cc23` are not re-opened.

### Architecture delta

Narrowed: no dependency-cruiser/madge. Derived from `Cargo.toml` at `8130f81` vs `b76cc23` plus `cargo test -p mediaops-arch-tests` (`live_workspace_is_ad2_subgraph` passed).

Workspace members did not change. Stub crates `net`, `ssh`, `store`, `transfer` gained implementations. New workspace pins: `sha2`, `libc`, `tokio-stream`, `tower`, `http`, `hyper-util`, `rustls-pki-types`. `mediaopsd` gained `mediaops-net` (not `store`/`encode`). `mediaops` gained `mediaops-ssh`, `mediaops-store`, `mediaops-transfer`. `transfer` depends on `net`+`proto` and at epic close was only a re-export door (`git show b76cc23:crates/transfer/src/lib.rs`). `arch-tests` was **not** edited in this range; the `mediaopsd → net` and `cli → transfer → net` edges were already on the AD-2 allow-list.

- **F1 — transfer is the AD-2 door into `net`.** Source: `b76cc23:crates/transfer/src/lib.rs` (`pub use mediaops_net::{… mint, probe_range_n}`). The CLI must not depend on `net` directly. **Accept as-is** (matches AD-2). Later epics filled `transfer` with PullFile; the door remains legal.

- **F2 — daemon stays store/encode-free.** Source: `b76cc23:bins/mediaopsd/Cargo.toml` (core+net only); HEAD `bins/mediaopsd/Cargo.toml` adds `arr` (Epic 5) still without store/encode; arch-tests closure check passed. **Checked and clean.**

No new crate cycles. No `native-tls` (`crates/arch-tests/src/lib.rs` `BANNED_DIRECT_CRATES`).

### Duplication map

- **F3 — `parse_root` copied in both binaries.** Source: `bins/mediaops/src/bootstrap.rs:63` and `bins/mediaopsd/src/main.rs:59` (identical `id=path` split). **Accept as-is** (composition-root clap parsers; not a second walker or mint path).

### God-class / size growth

Ranked on `git_evidence.py` `files` (change volume, not absolute size). `merges_measured == merge_count`; `binary_revisions` all 0.

At epic close, `bootstrap.rs` ~627 lines (including tests), `net/src/lib.rs` ~581 (mostly tests), `ssh/src/lib.rs` ~460, `seedbox.rs` ~210. Production types are split (`mint`, `listen`, `pool`, `seedbox`, `install_provider`). **No god-class at epic close.** HEAD growth (`bootstrap.rs` 1106, `seedbox.rs` 838, `ssh` 708, `gateway.rs` 658) is later epics, not this range.

### Pattern divergence

- **F4 — bootstrap orchestration lives in the CLI binary.** Source: `bins/mediaops/src/bootstrap.rs` at `d2d9331`/`b76cc23`; `SystemExec` injected from `bins/mediaops/src/main.rs` (`git show b76cc23:bins/mediaops/src/main.rs` uses `bootstrap::bootstrap(args, &SystemExec)`). Tests inject `TranscriptExec` (`mediaops-ssh`). Matches AD-1 (bin is composition root) and AD-16 (exec port). **Accept as-is.**

### Spec-to-implementation reconciliation

Declared ACs: `epics.md` stories 2.1–2.3. Spine ADs listed on the epic summary (FR10 daemon/mTLS slice, FR12, FR25, NFR1/3/10, AD-5/7/12/14/16/21/22).

| AC | As-built evidence | Disposition |
| --- | --- | --- |
| 2.1 rcgen ECDSA P-256 CA/server/client; rustls require-and-verify; no native-tls | `crates/net/src/mint.rs` (`PKCS_ECDSA_P256_SHA256`, `WebPkiClientVerifier`); test `mint_is_ecdsa_p256_and_fingerprints_are_sha256` | Met |
| 2.1 UDS+TCP same rustls config | `crates/net/src/listen.rs` `serve_tcp`/`serve_unix`; test `tcp_and_uds_share_rustls_config` | Met |
| 2.1 `endpoint_fingerprint` = hash(address + underlay) | `crates/core/src/probe.rs:38-44` blake3 | Met |
| 2.1 N independent TCP+TLS, one in-flight per channel | `connect_pool` + `ChannelPool::try_checkout` + `Endpoint::concurrency_limit(1)` (`listen.rs:174-175`, `pool.rs:39-47`) | Met |
| 2.1 tests without a live box | net tests bind `127.0.0.1:0` | Met |
| 2.2 seedbox binds Control+Transfer, walker-backed List/Stat/GetRange, `grabber=None` | `Seedbox` in `crates/net/src/seedbox.rs`; `mediaopsd serve --role seedbox --bind 0.0.0.0:50051` (`b76cc23:bins/mediaopsd/src/main.rs`) | Met |
| 2.2 no `store`/`encode` in mediaopsd; reverse-connect unused | Cargo.toml + `DaemonRole::ensure_seedbox`; tests `serve_reverse_connect_exits_usage` | Met |
| 2.2 Control carries semver + proto package | Wire `DfResponse` at epic close; `ControlPort::df` then returned only `Bytes` after `check_handshake` (`b76cc23:crates/proto/src/lib.rs` ~292–301). HEAD: `DfSnapshot` + `accept_handshake` (`crates/proto/src/lib.rs:557-567`, `crates/core/src/control_port.rs:55-61`). Review deferred the trait shape; later epics closed it. | **Accept as later-landed** (not an open Epic 2 item) |
| 2.3 import `Host seedbox`; mint into config dir; refuse git work tree; fingerprints not PEMs | `parse_ssh_config`; `refuse_git_work_tree`; `upsert_tls_table` + `looks_like_pem`; CLI tests | Met |
| 2.3 SwizzinBox / AlreadyThere; unimplemented fail loudly | `ProviderKind::ensure_installable`; ssh + CLI tests | Met |
| 2.3 probe N, persist by fingerprint, not every run | `probes` table (`store` v1 at epic close); reuse unless missing row (`bootstrap.rs:160-174`) | Met |
| 2.3 exec-port transcripts; bulk copy is a test failure | `TranscriptExec` + `reject_bulk_copy` (`crates/core/src/exec.rs`) | Met |
| 2.3 destructive steps surfaced before run | `--yes` gate → `NeedsConfirm` exit 5; CLI test `seedbox_bootstrap_without_yes_is_policy_refusal` | Met |
| 2.3 **builds** `x86_64-unknown-linux-musl` mediaopsd | Command is issued (`crates/ssh/src/lib.rs:117-132`). Actual link of `tls-aws-lc` on musl is unproven. No `.cargo/config.toml` in the tree. CI is host `cargo test --workspace` only (`.github/workflows/ci.yml:18-19`). Still on `deferred-work.md` L42 and `demo-epic-4.md`. | **F5 — defer + action item** |
| AD-12 home gateway owns the WAN pool; CLI never contains a seedbox address | Explicitly deferred to Epic 3 in `review-pr-4-epic-2.md`. Closed 2026-08-31 per `deferred-work.md` (home role + `ChannelPool`). Bootstrap still probes the seedbox address when the home socket is down (chicken-egg note in that ledger). | **Accept as later-landed** |
| AD-8 repo traits in `core` | Deferred at PR #4; closed 2026-08-30 (`ProbeRepo` in `core`). | **Accept as later-landed** |

- **F6 — GetRange is cap-then-buffer, not a disk pipe.** Source: `crates/net/src/seedbox.rs:429-462` (`read_to_end` up to `64 * 1024 * 1024`, then `chunks(64 * 1024)` into the tonic stream). Story 2.2 says “streaming GetRange”. Review deferred true `AsyncRead` + cancel backpressure (`deferred-work.md` L38; `review-pr-4-epic-2.md`). Wire still emits a stream. **Defer** (already ledgered).

### Diff-scope review (bmad-review inline)

Content class: **code/diff** (`d2d9331^..3d64fb0`). Lenses: adversarial, edge-case-hunter, verification-gap. Structure/prose skipped (`applies_to: docs`). Nested review subagents were not available; this is the recorded narrowing. Findings overlap with F5/F6 is signal, not a second defect.

#### Adversarial

The lens requires ≥10 concrete issues. Routed below; several are already deferred in `review-pr-4-epic-2.md` / `deferred-work.md`.

- **F5 (repeat) musl-static unproven.** `crates/net/Cargo.toml:20` `tls-aws-lc`; `crates/ssh/src/lib.rs:117-132`; no `.cargo/config.toml`; CI host-only. **Fix now.**
- **F6 (repeat) GetRange buffers the range.** `crates/net/src/seedbox.rs:429-462`. **Defer.**
- **F7 — TLS accept is sequential.** `crates/net/src/listen.rs:37-42` (`StreamExt::then` one handshake at a time). Handshake timeout 10s is present (`listen.rs:85`). Concurrent accept was deferred (`deferred-work.md` L39). **Defer.**
- **F8 — `parse_ssh_config` ignores `Include` / `Host *` / `Match`.** `crates/ssh/src/lib.rs:33-37`. v1 exact `Host seedbox` block. **Defer** (pre-existing review defer).
- **F9 — `upsert_tls_table` round-trips TOML and drops comments.** `crates/core/src/desired_state.rs:523-561` (`toml::from_str` / `toml::to_string`). **Defer** (AD-6 comment-preserving splice; already ledgered).
- **F10 — Partial `tls/` remints a new CA on AlreadyThere without placing server certs.** `bins/mediaops/src/bootstrap.rs:176-185` + `tls_bundle_on_disk` (`358-368`): any missing one of five files calls `mint()` + `write_to_dir`. `AlreadyThere` is a no-op install (`crates/ssh/src/lib.rs` `already_there_install`). Local CA can diverge from a remote daemon that still has the old server cert. No test covers a 4-file `tls/` dir. **Fix now.**
- **F11 — Probe samples the first `List` entry only, 1 MiB.** `crates/net/src/lib.rs:117-136`. A tiny first file under-samples WAN N. **Defer** (probe is a bootstrap heuristic; plateau tests use in-process files).
- **F12 — `scp` of `server.key` does not preserve 0600.** Local write is 0600 (`crates/net/src/mint.rs:226-228`). `scp_file_command` (`crates/ssh/src/lib.rs:135-144`) has no `-p`; remote mode is umask. **Defer.**
- **F13 — systemd-user enable without lingering.** `crates/ssh/src/lib.rs:373-381` `systemctl --user enable --now`. No `loginctl enable-linger` in the tree. Current session starts; boot persistence on a seedbox without lingering is unhandled. **Defer.**
- **F14 — `mediaopsd serve` is not executed as a process.** `bins/mediaopsd/tests/cli.rs` covers `--help`, bind default text, unused roles. In-process `serve_tcp` is tested in `mediaops-net`. A wiring regression in the binary `serve()` path would not fail those CLI tests. **Defer.**

#### Edge-case hunter

Unhandled paths reachable from the epic diff (handled paths discarded):

- Partial tls bundle remint (F10).
- `Host seedbox` with no `HostName` requires `--address` (`bins/mediaops/src/bootstrap.rs:414-421`) — handled.
- Empty probe listing errors (`lib.rs:122-124`) — handled.
- GetRange past EOF / oversize clamp — handled (`seedbox::tests::get_range_past_eof_is_empty_and_oversize_is_clamped_to_remain` at HEAD).
- `Port` parse uses `.ok()` and can silently drop a bad ssh `Port` (`parse_ssh_config`). Probe address is `:50051` anyway (`resolve_grpc_address`). Not a GetRange bug. **Accept as-is.**
- Deletion check: no retired contract. native-tls remains banned. Walker symlink follow was **added** as a guard in `b76cc23` (`join_unfollowed` in `crates/core/src/walker.rs`), not removed.

#### Verification-gap

- **F5 (repeat) — regression/broken-verification on musl.** `TranscriptExec` returns status 0 for `cargo` without compiling (`crates/ssh/src/lib.rs` tests `swizzin_copies_binary_and_unit_not_a_tree`; `bins/mediaops/src/bootstrap.rs` `swizzin_scp_uses_musl_artifact_path`). Demonstration: `musl_build_command` could point at a target that does not link; no test would fail. Searched: `x86_64-unknown-linux-musl` only in ssh command/path, bootstrap plan string, deferred-work, demo runbook — no CI job. `gap_shape: broken-verification-gap`. Consumer: SwizzinBox install (`crates/ssh/src/lib.rs:352` path via `musl_build_command`).
- **F15 — live bootstrap never verified.** Story 2.3 allows touching SeedIt4Me; this retro did not; epic commits have no live test. `bins/mediaops/tests/live.rs` is later, feature-gated, and does not dial. `gap_shape: regression-gap` for “health after bootstrap is gRPC on this box” as a live property. Offline transcript coverage of the plan/`--yes` gate is real. **Defer** (AD-20: live is not default CI).

`pool_is_n_independent_channels` (`crates/net/src/lib.rs` tests) asserts `len==3` and N+1 exhausted. Distinct TCP is implied by `connect_pool`’s N `connect_tcp` calls. Epic 3 added `tcp_connect_count` for the gateway AD-12 test; not an Epic 2 hole against the 2.1 AC (primitive + unit tests, no live box).

## Behavior verification

Runtime behavior **was** exercised offline. The live SeedIt4Me box was **not** touched. `MEDIAOPS_LIVE` was not set. musl target was **not** built.

Commands (host toolchain, `--offline`, 2026-09-02):

| Command | Result |
| --- | --- |
| `cargo test -p mediaops-net -p mediaops-ssh -p mediaops-proto -p mediaopsd -p mediaops-arch-tests -p mediaops-store --offline` | net 23 ok; ssh 11 ok; proto 21 ok; arch-tests 14 ok; store 20 ok; mediaopsd unit 7 ok; mediaopsd `tests/cli.rs` 12 ok |
| `cargo test -p mediaops-core --offline` | 136 ok (includes `probe::`, `provider::`, `exec::`, walker symlink-escape / open) |
| `cargo test -p mediaops --offline bootstrap` | binary unit 13 ok (`yes_already_there_skip_probe_mints_once`, `swizzin_scp_uses_musl_artifact_path`, git/lock/skip-probe); `tests/cli.rs` filter also matched 4 tests including `seedbox_bootstrap_*`; `tests/live.rs` ran **0 tests** (no `live-box` feature) |

Not run: `cargo build --target x86_64-unknown-linux-musl`; `make test-live`; any SSH to Host seedbox.

## Previous-retro follow-through

Nothing to follow through on, for both reasons the rubric names:

1. No prior retro document (`{implementation_artifacts}/**/epic-1-retro-*.md` absent; batch start).
2. `sprint-status.yaml` has no `action_items` key.

No `--set-action-status` transitions would have been proposed even in an interactive run. Headless: that flag is not passed.

## Action items

Proposed, not applied. Parent orchestration forbade `sprint_status.py update` and any `sprint-status.yaml` edit; these are not appended to sprint-status.

1. **Prove musl-static `mediaopsd` actually links** for `x86_64-unknown-linux-musl` with the `tls-aws-lc` backend; add `.cargo/config.toml` (or the linker flags the first musl build needs) and an offline CI/make gate that fails if that target cannot build. Source: F5 / Story 2.3 AC / `deferred-work.md` L42. Kind: remediation. **Owner: Coto**
2. **Refuse remint when `tls/` is a partial bundle** (or copy server material on AlreadyThere remint) so a 4-file dir cannot mint a new CA that the remote daemon does not have. Source: F10. Kind: remediation. **Owner: Coto**
3. **Name story keys in commit subjects** (`2-1`, `2-2`, `2-3`) and write `{implementation_artifacts}` story files the way Epic 1 did, so `git_evidence.py --stories` can attribute. Source: empty `stories` arrays in this run. Kind: process. **Owner: Coto**

## Acceptance verdict

**accepted-with-open-items** (declared criteria).

- Completeness: `pending_stories` empty; three stories `done`. Machine rejected-for-unfinished does not apply.
- Criteria were **declared** in `epics.md` 2.1–2.3, not profiled.
- Offline evidence shows mTLS mint, require-and-verify, UDS+TCP, channel pool, seedbox Control+Transfer over the walker, `grabber=None`, provider loud-failure, Host seedbox import, git-work-tree refusal, fingerprint-only desired-state, probes keyed by `endpoint_fingerprint`, `--yes` gate, exec-port transcripts, bulk-copy refusal.
- Open tracked items: F5 (musl unproven), F6 (GetRange buffer), F7–F9 / F11–F15 (deferred), plus action item 2 (partial tls remint). None of those make the implemented ACs false except the **unverified** musl link, which the review already deferred as a pin-when-first-built item rather than a missing command.
- Later epics closed the PR #4 defers for home-gateway pool, `ControlPort` handshake/`DfSnapshot`, and store traits in `core`. Those are not open on this retro.
- No human override (headless).

## Open questions

- Should the musl gate be a CI job on every PR, or a documented operator one-shot plus `make` target? (Changes how action item 1 is sized.)
- Does the SeedIt4Me box already have `loginctl linger` for the install user? (F13 is moot if it does.)
- Was live `seedbox bootstrap --yes` ever run outside git? No commit or session log says so.

## Assumptions

Headless choices made without the user:

- Epic selected from invocation: **2**. Confirmed with `detect-epic --epic 2` → `{"epic": 2, "story_count": 3, "pending_stories": [], "retro_key": "epic-2-retrospective", "retro_status": "optional"}`.
- `pending_stories` empty; no unfinished-story confirmation to invent.
- Machine verdict **accepted-with-open-items** rendered with no human decision.
- Diff range `d2d9331^..3d64fb0` (first/last commits of the epic window including sprint-row close).
- Story specs taken from `epics.md` because `spec-2-*` files do not exist.
- Session-log process analysis skipped (absent).
- `project-context.md` absent; continued.
- Phase 3 team discussion skipped (headless).
- bmad-review run **inline** (no nested subagents); lenses adversarial / edge-case / verification-gap only.
- Live SeedIt4Me not contacted; musl target not built.
- Action items 1–3 proposed above; not written to sprint-status.
- Phase 5 `sprint_status.py update --set-retro-done` **not run**, per parent orchestration. Retro key remains `optional`.
