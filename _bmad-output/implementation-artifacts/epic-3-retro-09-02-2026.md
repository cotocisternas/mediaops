---
epic: 3
date: 09-02-2026
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 3 retrospective — Home disk can receive a file

## Epic summary

Epic 3, **Home disk can receive a file**, as declared in `_bmad-output/planning-artifacts/epics.md` (lines 188–190, 402–479). Sprint-status keys `3-1-home-gateway`, `3-2-transfer-pull-verify-resume`, and `3-3-library-bootstrap-and-scheduler` are all `done`; `pending_stories` is empty. `epic-3-retrospective` is `optional` in `sprint-status.yaml` (this run does not flip it).

**Diff range:** `5813ab1^..5813ab1` (`5813ab1f72a743fa9ab2623a33f096e4bf297c71`). Single non-merge commit, subject: `Stand up the home gateway, resumable range pull, and library bootstrap for Epic 3.` `git_evidence.py --stories 3-1,3-2,3-3` returned `stories: []` for that commit: the subject names “Epic 3” but not the story ids, so per-story attribution is empty. `commit_count` 1, `merge_count` 0, `merges_measured` 0, `binary_revisions` 0 on every path. 34 files, +3310 / −109.

Nearby store commits `0fb6869` (“Add job state machines and migrate store to schema v2”) and `9fc22a6` (“Give jobs a title_id and make store writes atomic”) were **not** included. Their subjects do not name Epic 3 or 3-1/3-2/3-3. `_bmad-output/implementation-artifacts/deferred-work.md` (lines 19–31) attributes them to the story 1.3 follow-up (`core::jobs` + store schema v2/v3). Epic 3 only adds store schema v4 `machine` kv (`crates/store/src/machine.rs`, `crates/store/src/lib.rs` at `5813ab1`).

HEAD has since moved (Epics 4–6). This retro judges Epic 3’s declared ACs against the as-built at `5813ab1` and notes later drift only when it changes whether a hole is still open.

### Evidence inventory

| Artifact | Status |
| --- | --- |
| Epic spec + declared ACs | `_bmad-output/planning-artifacts/epics.md` Epic 3; FRs FR9, FR13, FR16 (lock exists), FR25, NFR4, NFR6, NFR8; ADs AD-4, AD-11, AD-12 |
| Architecture / SPEC | `ARCHITECTURE-SPINE.md` AD-4, AD-8, AD-11, AD-12, AD-13, AD-20; `failure-history-tests.md`; `increments.md` |
| Story files | **Missing** as `{implementation_artifacts}/spec-3-*.md`. Intent lives only in `epics.md` stories 3.1–3.3 |
| Diff range / git_evidence | `5813ab1^..5813ab1`; JSON measured; story ids unmatched |
| Sprint status | `epic-3: done`; three stories `done`; no `action_items` key |
| Previous retrospective | **None** (`epic-*-retro-*.md` absent; batch start) |
| Session logs | **Absent** — process-lesson analysis from sessions skipped |
| `project-context.md` | **Absent** (glob `{project-root}/**/project-context.md`) |
| bmad-review | Ran **inline** over this epic’s diff (adversarial, edge-case, verification-gap). No nested subagents. Editorial lenses not applicable to a code diff |

Top churn (`git_evidence.py` `files`, non-merge): `crates/net/src/gateway.rs` +611; `crates/transfer/src/pull.rs` +361; `bins/mediaops/src/home.rs` +341; `bins/mediaops/src/library.rs` +220; `crates/sync/src/lib.rs` +209; `crates/core/src/install.rs` +170/−61; `bins/mediaops/src/main.rs` +150; `crates/transfer/src/home.rs` +145.

## Findings

### Aggregate views

#### Architecture delta

Derived from `5813ab1` vs `5813ab1^` Cargo.toml files and the workspace graph. No language-native dep graph tool was run (narrowed to Cargo edges + `crates/arch-tests`).

- New workspace crates used by this epic: `mediaops-encode` (NVENC `ffmpeg -encoders` probe only at `5813ab1`) and `mediaops-sync` (layout, watermark, systemd unit text, media-server warning scan). CLI gained `mediaops-encode` and `mediaops-sync`. `mediaopsd` gained `directories` for the home socket default. `proto` grew a home-only `Gateway` service (`ConfigurePool`, `PoolStatus`, `ProbeRange`).
- Legal AD-2 edges held: `mediaopsd` still does not link `store` or `encode`; CLI still reaches `net` only through `transfer`. HEAD `cargo test -p mediaops-arch-tests` — `live_workspace_is_ad2_subgraph` passed.
- UDS overlay: `connect_unix` dropped `.concurrency_limit(1)` (`crates/net/src/listen.rs` at `5813ab1`) so many Range RPCs multiplex on the unix socket; WAN pinning is the pool. Matches AD-12’s overlay/WAN split.
- **AD-1 tension (accept as-is for v1 composition):** `bins/mediaops/src/home.rs` (+341) and `library.rs` (+220) hold pull/bootstrap orchestration (probe lookup, watermark arithmetic, job create/advance, placement, JSON). AD-1 wants that logic in library crates. Transfer/sync own the kernels; the binaries are thicker than the spine’s “parse/lock/call/render” line.
- Home gateway opens a dedicated Control TCP (`HomeGateway::connect` → `connect_tcp`) **plus** the N-channel Range pool. AD-12’s N is the Range pool; the extra control channel is unspecified. Accept as-is: Control is not GetRange.

#### Duplication map

- In-process `scratch()` + `write_file` + `NET_TEST` serial mutex copied in `crates/net/src/gateway.rs`, `crates/transfer/src/home.rs`, `crates/transfer/src/pull.rs`, `crates/transfer/src/prune.rs`, `crates/sync/src/lib.rs`, `bins/mediaops/src/bootstrap.rs`. Same pattern as Epic 2 net tests — not independent reimplementation of PullFile.
- `map_bootstrap` copied in `home.rs`, `library.rs`, and `run.rs` at `5813ab1`.
- Whole-file BLAKE3 runs in `pull_file` (`hash_file`) and again in `install` (AD-11 install gate). Intentional double-hash, not a fork of algorithms.

No second `.partial` format and no second staging-path renderer: `staging_path` from `core` is the only tail; pull appends `.partial` / `.partial.b3`.

#### God-class / size growth

`gateway.rs` +611 is the ranking leader, but at `5813ab1` that file is ~272 lines of proxy + Gateway RPCs and the rest tests. Not a god class: Control methods are pass-through, Transfer::get_range pins a `SlotGuard`, Gateway owns Configure/Status/Probe.

`bins/mediaops/src/home.rs` +341 is the real size smell (orchestration in the binary). HEAD grew it to 630 lines in later epics. `pull.rs` +361 is one function plus tests and stays cohesive.

#### Pattern divergence

- **AD-20 fake-transport vs loopback tonic.** AD-12/AD-20 ask for a fake-transport transcript that N concurrent streams produce N distinct upstream connections. Epic 3 tests bind `127.0.0.1:0` and a real `HomeGateway`. `rg fake_transport crates/net` is empty. Accepted deviation: in-process tonic is the suite’s transport fake; the remaining hole is what the tests *assert* (see verification-gap).
- **NVENC cap is encoder presence 0/1** (`crates/encode/src/lib.rs` `cap: u32::from(hevc)`). Deferred in `deferred-work.md` lines 50–51; story 3.3 required “this box’s live cap is config, not a hardcoded 8” — they persist a probe, not `8`. Epic 4 later added `session_cap`.
- **Sidecar digest is `String`**, not `Blake3Hex` (`crates/transfer/src/sidecar.rs:25`). Load does not require hex.
- **Pull job row is created after `pull_file` succeeds** (`bins/mediaops/src/home.rs:233-248`). Crash during copy has sidecar + `.partial` (story 3.2) but no `jobs` row (AD-10). Accept for Epic 3: recovery is sidecar-first; planner jobs wait for Epic 4.

#### Spec-to-implementation reconciliation

Declared ACs from `epics.md` 406–479, checked against `5813ab1`:

| AC | As-built | Disposition |
| --- | --- | --- |
| 3.1 home binds UDS, holds client cert, re-serves Control+Transfer | `mediaopsd` `--role home` → `serve_home` → `serve_home_unix` adds Control+Transfer+Gateway | met |
| 3.1 proxy Status byte-for-byte; never writes staging/library | tonic client `.await` returns upstream `Status`; daemon has no `store`/install | met (test only checks details non-empty) |
| 3.1 ConfigurePool 1:1 pin; N+1 `ResourceExhausted` | `ChannelPool::try_checkout`; `REASON_RESOURCE_EXHAUSTED` | met |
| 3.1 AD-12 test: N streams → N connections + `endpoint_fingerprint` | fingerprint on `PoolStatus`; N TCP opens on ConfigurePool; **not** fake-transport; concurrent streams through the gateway are not counted | deferred (verification) |
| 3.2 `.partial` + versioned `.partial.b3`; resume uses sidecar `range_len` | `Sidecar` v1; `resume_skips_recorded_ranges_and_uses_sidecar_range_len` | met |
| 3.2 range complete only after fsync + hash recorded | `write_range` `sync_all` then `sidecar.record` then `save` (`sync_all` on tmp) | met |
| 3.2 `*.partial*` dirs sacred to prune | `dir_is_sacred` + `sidecar_only_dir_is_sacred_and_not_pruned` | met (see edge-case on `contains`) |
| 3.2 files-first then split largest | `take_slots` unit-tested; `pull_file` always passes a **one-file** array (`pull.rs:106-111`) | accept: Epic 3 CLI pull is one file; scheduler is ready |
| 3.2 size/mtime never proof; whole-file BLAKE3 at install | range + whole-file BLAKE3; `install` returns `InstallOutcome.whole_file_b3` | met |
| 3.3 schema dirs + `_ops`/`_incoming`; refuse below `min_free`; no media-server config | `ensure_layout`, `refuse_below_watermark`; warnings only | met |
| 3.3 oneshot + `OnUnitInactiveSec` + flock; no `OnCalendar`; lock JSON pid/started_at/command | `run_timer_unit`, `exclusive_lock` | met |
| 3.3 NVENC probe persisted; tests fake exec | `put_machine("nvenc_cap")`; encode tests use `ExecPort` fake | met, with 0/1 cap deferral |
| 3.3 `title_index` ready; `install` only library-path writer besides `replace` | `Store::open` creates tables; `fs::rename` into schema paths only in `install.rs` | met |

Review-ledger items from `deferred-work.md` 44–51 (Epic 3 working tree): unbounded `prune_dir` recursion; no `mediaopsd --role home` unit at delivery; NVENC 0/1. Epic 4 commit `ee76a85` later added `write_home_unit` (`mediaopsd-home.service`). That is later-epic work, not previous-retro follow-through.

Chicken-egg seedbox probe still may dial WAN directly when the home socket is down (`deferred-work.md` 35; `bins/mediaops/src/bootstrap.rs`). List/pull take only `--socket` / `--tls-dir` / `--config-dir`.

### Diff-scope review (bmad-review inline)

Content class: code diff (`5813ab1^..5813ab1`). Lenses: adversarial, edge-case-hunter, verification-gap. Structure/prose skipped (`applies_to: docs`). **Narrowing:** no nested review subagents; HEAD-only files were not treated as epic-introduced except where named as later drift.

#### Adversarial

1. **location:** `crates/transfer/src/pull.rs:165`  
   **trigger_condition:** Resume allocates `vec![0_u8; range.len as usize]` from sidecar JSON.  
   **guard_snippet:** Reject `offset.checked_add(len)` outside `file_len`, and `len > remaining` / `len as usize` overflow, before allocate.  
   **potential_consequence:** A crafted `.partial.b3` OOMs the pull process.

2. **location:** `crates/net/src/gateway.rs:64-74`  
   **trigger_condition:** `configure` drops the pool lock, `connect_pool`s, then replaces the pool.  
   **guard_snippet:** Hold the lock across compare-and-swap, or refuse ConfigurePool while slots are checked out.  
   **potential_consequence:** Concurrent ConfigurePool races; in-flight streams keep old Arcs but N is a lie on `PoolStatus`.

3. **location:** `crates/transfer/src/pull.rs:15-21` `RangeSource::get_range` → `Vec<u8>`  
   **trigger_condition:** Each in-flight range is fully materialized in CLI memory.  
   **guard_snippet:** Stream into the `.partial` file (known Epic 2 GetRange-is-not-a-pipe deferral).  
   **potential_consequence:** Probe `max_n=32` × default 64 MiB ≈ 2 GiB RAM.

4. **location:** `crates/transfer/src/sidecar.rs:25`  
   **trigger_condition:** `blake3` is an unvalidated `String`; serde allows unknown fields.  
   **guard_snippet:** `Blake3Hex` + `deny_unknown_fields`.  
   **potential_consequence:** Tampered sidecar records a range as complete until byte compare fails.

5. **location:** `crates/transfer/src/prune.rs:18`  
   **trigger_condition:** Sacredness is `name.contains(".partial")`.  
   **guard_snippet:** Match `*.partial` / `*.partial.b3` suffixes (AD-11).  
   **potential_consequence:** `note.partially.txt` blocks prune; `*.PARTIAL` does not.

6. **location:** `crates/sync/src/lib.rs:201`  
   **trigger_condition:** Media-server warning is a substring search in xml/json/conf/yml.  
   **guard_snippet:** Parse known Jellyfin/Plex library paths, or document the heuristic.  
   **potential_consequence:** False warning or a real `_incoming` library missed (non-xml config).

7. **location:** `crates/transfer/src/pull.rs:70-77`  
   **trigger_condition:** Resume with sidecar `file_len != spec.file_len` is a hard error.  
   **guard_snippet:** Named error + operator path to discard sidecar.  
   **potential_consequence:** Remote size change wedges resume until manual delete.

8. **location:** `crates/transfer/src/pull.rs:149`  
   **trigger_condition:** `let _ = prune_empty_incoming(...)` swallows prune errors.  
   **guard_snippet:** Surface or log `TransferError`.  
   **potential_consequence:** Empty title dirs accumulate with no signal.

9. **location:** `bins/mediaops/src/library.rs` `--enable-timer` (default false)  
   **trigger_condition:** Units are written; `daemon-reload`/`enable --now` only if `--enable-timer`.  
   **guard_snippet:** Document that files on disk ≠ enabled timer, or reload without enable.  
   **potential_consequence:** Operator thinks the timer exists in systemd when only unit files were written.

10. **location:** `bins/mediaops/src/home.rs:233-248`  
    **trigger_condition:** Job `Start`/`FinishRanges` recorded after the copy already finished.  
    **guard_snippet:** Create the pull job before `pull_file` (Epic 4 apply does this better).  
    **potential_consequence:** `jobs` cannot explain an in-flight copy; sidecar can.

11. **location:** `crates/encode/src/lib.rs:50-52`  
    **trigger_condition:** Persisted cap is 0 or 1 from `hevc_nvenc` text, not sessions.  
    **guard_snippet:** Already deferred (`deferred-work.md:50-51`).  
    **potential_consequence:** Later encode over-subscribes the GPU if it trusted this cap as N.

#### Edge-case hunter

Unhandled or weakly handled paths reachable from the diff:

- **location:** `crates/transfer/src/sidecar.rs:55-71` — load does not reject overlapping ranges or `offset+len > file_len`. Guard: validate against `file_len` on load. Consequence: `remaining()` can skip a planned range that only partially overlaps a bogus sidecar row.
- **location:** `crates/transfer/src/pull.rs:157-160` — missing `.partial` clears in-memory ranges but does not rewrite the sidecar until the next `save`. Guard: `sidecar::save` after clear. Consequence: kill before first new range leaves a lying sidecar next to no partial; next resume re-clears (works) but the file on disk is stale.
- **location:** `crates/transfer/src/prune.rs:33-37` — `prune_dir` recursion unbounded (`deferred-work.md:48`). Guard: depth cap. Consequence: stack overflow on a deep `_incoming` (operator-local today).
- **location:** `crates/net/src/gateway.rs:229-231` — upstream `get_range` error returns `Err(status)` and drops `slot` (handled). No finding.
- **location:** `bins/mediaops/src/home.rs` `TitleKind::Album` `--install` — usage error, staged file kept. Handled.
- **Deletion:** UDS `.concurrency_limit(1)` removed. Contract re-established by WAN pool + `two_overlapping_uds_get_ranges_complete_when_pool_is_n`. No deletion finding.

#### Verification-gap

1. **gap_shape:** regression-gap  
   **location:** AD-12 / `crates/net/src/gateway.rs` GetRange pin  
   **trigger_condition:** Gateway tests never show N concurrent *proxied* streams producing N WAN connections.  
   **consumer:** `configure_pool_opens_n_wan_channels_and_refuses_n_plus_one` (`gateway.rs:443`) counts `tcp_connect_count()` around ConfigurePool, then holds slots via test-only `checkout_slot` and asserts N+1 `ResourceExhausted`. `pool_is_n_independent_channels` (`crates/net/src/lib.rs:378`) runs 3 GetRanges on a raw `ChannelPool`, not through `HomeGateway`. `two_overlapping_uds_get_ranges_complete_when_pool_is_n` does not count WAN connects.  
   **evidence:** Read those three tests; `rg fake_transport crates/net` empty.  
   **potential_consequence:** A gateway that multiplexed Range onto `self.control` would still pass ConfigurePool’s connect-count test.  
   **guard_snippet:** N UDS GetRange while measuring N distinct pool checkouts/connects; name it after “Collapsing all Range RPCs onto one TCP”.

2. **gap_shape:** missing-adoption-gap  
   **location:** `crates/transfer/src/pull.rs:106-111`  
   **trigger_condition:** `take_slots` multi-file behavior is unused by the only production caller.  
   **consumer:** `pull_file` builds a 1-element `PendingFile` array. `take_slots` is not called from `crates/sync` (`rg take_slots`).  
   **evidence:** `schedule.rs:74-88` unit test vs `pull.rs:106-111`. Supersession: story 3.2 AC “when multiple files and ranges are in flight”. Shared contract: the scheduler.  
   **potential_consequence:** A later multi-file loop that forgets `take_slots` still compiles; no pull test fails.

3. **gap_shape:** regression-gap  
   **location:** CLI `mediaops pull` / `list`  
   **trigger_condition:** No binary test drives list/pull over UDS.  
   **consumer:** `bins/mediaops/tests/cli.rs` at `5813ab1` added `library_bootstrap_creates_schema_dirs` and `run_is_policy_refusal_until_epic_4` only.  
   **evidence:** `git show 5813ab1:bins/mediaops/tests/cli.rs`; transfer `home.rs` tests `connect_home` list/stat/range, not CLI argv.  
   **potential_consequence:** CLI wiring (lock, probe persist, `--install`) can regress without `cargo test -p mediaops --test cli` failing.

4. **gap_shape:** regression-gap  
   **location:** `bins/mediaops/tests/cli.rs:300` `library_bootstrap_creates_schema_dirs`  
   **trigger_condition:** Fixture sets `min_free_gib = 0`; does not read `state.db`.  
   **consumer:** CLI bootstrap JSON/`title_index`/`machine.nvenc_cap`.  
   **evidence:** Test asserts dirs + timer files + canonical `library_root`. No `nvenc_cap` JSON assert; no sqlite. Watermark unit test is `watermark_refuses_when_min_free_is_impossible` (u64::MAX), not the CLI.  
   **potential_consequence:** Dropping `put_machine("nvenc_cap")` or `title_index` create would not fail this CLI test.

5. **gap_shape:** broken-verification-gap  
   **location:** `proxy_forwards_upstream_status_details` (`gateway.rs:528`)  
   **trigger_condition:** Asserts `!err.details().is_empty()`, not code/message/details equality with the seedbox status.  
   **consumer:** Story 3.1 byte-for-byte proxy AC.  
   **evidence:** Test body at `5813ab1` / HEAD `gateway.rs:528-551`.  
   **potential_consequence:** A re-wrap that keeps some details still passes.

6. **gap_shape:** regression-gap  
   **location:** failure-history rows “CLI process contains a seedbox address”, “Size/mtime treated as proof”  
   **trigger_condition:** No test **name** cites those rows (AD-20).  
   **evidence:** `failure-history-tests.md:47-49`; `rg` on test names. Behavior is mostly implemented (list/pull have no `--address`; hashes not mtime).  
   **potential_consequence:** AD-20 mapping decays even when the code is right.

7. **gap_shape:** other  
   **location:** live PullFile  
   **trigger_condition:** Live seedbox pull/resume was not run (and must not be in this retro).  
   **evidence:** `bins/mediaops/tests/live.rs` is `#[cfg(feature = "live-box")]` and ignored without `MEDIAOPS_LIVE=1`; this run did not pass `--features live-box`.

## Behavior verification

Offline only. No live SeedIt4Me pull. No write to a real library (CLI bootstrap used a tempdir). `--features live-box` was not enabled. `tests/live.rs` compiled empty (0 tests).

| Command | Result |
| --- | --- |
| `cargo test -p mediaops-net --lib` | 23 passed |
| `cargo test -p mediaops-store --lib` | 20 passed (HEAD includes later v5 holds migrations) |
| `cargo test -p mediaops-sync --lib` | 38 passed (HEAD includes Epic 4+ apply/plan, including `partial_resume_still_works_when_apply_is_killed_mid_file`) |
| `cargo test -p mediaops-transfer --lib` | 17 passed (pull, resume, sidecar `range_len`, prune sacred, UDS list/stat/range) |
| `cargo test -p mediaops-core --lib install` | 22 passed (install gate + jobs filter) |
| `cargo test -p mediaops-arch-tests` | 14 passed, including `live_workspace_is_ad2_subgraph` |
| `cargo test -p mediaops --test cli library_bootstrap` | 1 passed — tempdir schema dirs + units, not a real library |
| `cargo test -p mediaopsd --test cli serve_home` | 1 passed (`serve_home_without_upstream_exits_usage`) |
| `cargo test -p mediaops-encode cap_` | 4 passed (fake `ExecPort`, no GPU) |
| `cargo test -p mediaops exclusive_lock` | 2 passed |
| `cargo test -p mediaops enable_timer` | 2 passed (fake systemctl) |

**Not exercised:** live Range pull, kill-at-90% resume against the seedbox, CLI `pull` argv over a running home gateway, `library bootstrap --enable-timer` against real systemd-user.

Observed: in-process gateway proxies Control+Range; ConfigurePool(3) opens 3 TCP connects and refuses N+1; Mem `RangeSource` pull writes staging, hashes, resumes on sidecar `range_len`, refetches corrupt ranges; sidecar-only dirs are not pruned; library bootstrap CLI creates `movies`/`series`/`music`/`_ops`/`_incoming` and `mediaops-run.{service,timer}` without `OnCalendar`.

## Previous-retro follow-through

Nothing to follow through on:

- No prior retro document (`{implementation_artifacts}/**/epic-*-retro-*.md` absent; this is the batch start).
- `sprint-status.yaml` has **no** `action_items` key (only a comment that retros append to `action_items`).

No `--set-action-status` transitions would have been proposed even if this run were allowed to write sprint-status.

## Action items

Proposed only — not applied. Parent forbade `sprint_status.py update`.

1. **Remediation — sidecar range bounds.** In `verify_recorded_ranges` / `Sidecar::load`, reject ranges whose `offset+len` exceeds `file_len` or whose `len` cannot fit `usize` before allocating. Owner: Coto. Source: adversarial #1, edge-case load validation.
2. **Remediation — AD-12 gateway stream test.** Add a test that N concurrent GetRange RPCs *through* `HomeGateway` produce N WAN checkouts/connections and cite “Collapsing all Range RPCs onto one TCP”. Owner: Coto. Source: verification-gap #1, story 3.1 AC.
3. **Process — story ids in commit subjects.** Land story-scoped commits (or name `3-1`/`3-2`/`3-3` in the subject) so `git_evidence.py --stories` can attribute. Owner: Coto. Source: `stories: []` on the only Epic 3 commit.

Deferred (not action items): prune depth (`deferred-work.md:48`); NVENC 0/1 (`deferred-work.md:50-51`, later `session_cap` in Epic 4); media-server substring heuristic; CLI pull binary test; `let _ = prune_empty_incoming`; ConfigurePool TOCTOU; `--enable-timer` default.

## Acceptance verdict

**accepted-with-open-items** (declared criteria, machine verdict, no human override).

- `pending_stories` is empty — unfinished-story fail-safe does not fire.
- Stories 3.1–3.3 ACs are met in `5813ab1` with the deviations recorded above (loopback instead of fake-transport; files-first unit-tested but single-file `pull_file`; NVENC 0/1 instead of session cap).
- Offline tests of transfer, net, store, sync layout, install, library bootstrap CLI, and NVENC fake-exec passed.
- Named findings remain open (sidecar bounds, AD-12 gateway stream proof, process attribution, plus already-ledgered prune/NVENC items). None of those, on the evidence, falsify the epic’s “home disk can receive a file” claim under the declared ACs.

Not **accepted**: open deferred/fix-now items exist. Not **rejected**: criteria are demonstrably met and every story is `done`.

## Open questions

- Treat in-process loopback tonic as the AD-20 “fake-transport transcript,” or still require a non-TCP fake that records connection ids?
- Should `mediaops pull` get a CLI-level UDS test (in-process seedbox+gateway, temp library) in default CI?
- Is Epic 4’s `write_home_unit` / `session_cap` enough to close the Epic 3 review-ledger rows, or do those rows stay until someone ticks `deferred-work.md`?

## Assumptions

Headless; no user confirmations.

- Epic selected from invocation: **3** (not auto-detect). Detect-epic already reported `{"epic": 3, "story_count": 3, "pending_stories": [], "retro_key": "epic-3-retrospective", "retro_status": "optional"}`; this run did not re-exec `sprint_status.py`.
- `pending_stories` empty — machine verdict not forced to rejected.
- Verdict **accepted-with-open-items** is the machine call with no human decision.
- Diff range `5813ab1^..5813ab1`; `0fb6869` / `9fc22a6` excluded as 1.3 store follow-up.
- Phase 3 team discussion skipped (headless).
- bmad-review ran inline; no nested subagents.
- Session-log process analysis skipped (logs absent).
- `project-context.md` absent; continued.
- Behavior check = offline cargo tests listed above; live pull/resume not exercised.
- Proposed action items 1–3 (owners Coto) recorded, not executed.
- Parent-orchestration: did **not** run `sprint_status.py update`, did **not** edit `sprint-status.yaml`; retro key remains `optional`.
- No prior retro and no `action_items` array — nothing to `--set-action-status`.
- Document-only write: `_bmad-output/implementation-artifacts/epic-3-retro-09-02-2026.md`.
