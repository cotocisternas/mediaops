---
epic: 1
date: 09-02-2026
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 1 retrospective: The laws live in types

## Epic summary

Epic 1, "The laws live in types." After this epic a developer can compile the workspace, CI fails illegal crate edges, and identity / paths / plans / the wire contract are tested offline. No live box, no GPU.

- **Diff range:** `9050b50^..8130f81` (`f0f2339..8130f81`). First implementation commit `9050b50` Scaffold the Cargo workspace; last epic-1 commit `8130f81` Close the story 1.4 review findings. The left endpoint is the parent of the first story commit so the scaffold is in the range.
- **git_evidence.py:** `--repo /home/coto/work/dev/mediaops --range 9050b50^..8130f81 --stories 1-1,1-2,1-3,1-4` → `commit_count` 14, `merge_count` 3, `merges_measured` 3, `binary_revisions` 0. `files` and `merge_files` share the same paths; no merge-only churn.
- **Stories completed (sprint-status, all `done`):**
  - `1-1-workspace-scaffold-and-dependency-law`
  - `1-2-pathschema-titleid-and-the-walker`
  - `1-3-desiredstate-plan-jobs-and-store`
  - `1-4-wire-contract-in-the-proto-crate`
- **Unfinished stories (`pending_stories`):** none. `detect-epic --epic 1` returned `story_count: 4`, `pending_stories: []`, `retro_key: epic-1-retrospective`, `retro_status: optional`.
- **Criteria:** declared in `_bmad-output/planning-artifacts/epics.md` § `## Epic 1` (Given/When/Then ACs per story). Profiled fallback was not used.

### Evidence inventory

| Artifact | Status | Source |
| --- | --- | --- |
| Epic spec + declared ACs | present | `_bmad-output/planning-artifacts/epics.md:212–323` |
| Epic context | present | `_bmad-output/implementation-artifacts/epic-1-context.md` |
| Story specs | present, all `status: done` | `spec-1-1-…md`, `spec-1-2-…md`, `spec-1-3-…md`, `spec-1-4-…md` |
| Architecture | present | `_bmad-output/planning-artifacts/architecture/architecture-mediaops-2026-08-29/ARCHITECTURE-SPINE.md` (AD-1–AD-3, AD-8–AD-10, AD-13, AD-17–AD-20; Structural Seed) |
| SPEC | present; FR numbers live in `epics.md`, not `SPEC.md` | `_bmad-output/specs/spec-mediaops/SPEC.md` (CAP language); FRs at `epics.md:57–109` |
| Sprint status | present | `_bmad-output/implementation-artifacts/sprint-status.yaml` (`epic-1: done`, four stories `done`, `epic-1-retrospective: optional`) |
| Diff range + git_evidence | present | range above; JSON from `git_evidence.py` |
| Previous retrospective | **absent** | no `epic-*-retro-*.md` under implementation-artifacts |
| `action_items` in sprint-status | **absent** | file has the status-definition comment only; no `action_items:` key |
| Session logs for 1.1–1.4 | **absent** | `_bmad-output/specs/spec-mediaops/ARCHITECTURE-SESSION.md` is planning, not a story session |
| `project-context.md` | **absent** | glob `{project-root}/**/project-context.md` matched nothing |
| Nested `bmad-review` subagents | **unavailable** | parent-orchestration forbids nested subagents; lenses run inline over this epic's diff |

### Per-story commits

`git_evidence.py` attributes a commit only when the subject names a supplied story id. In this range that matched **two merge commits** (`3529ed0` → `1-2`, `389d571` → `1-3`). Subjects for 1-1 and 1-4 never contain `1-1` / `1-4` (1.4 uses `story 1.4` with a period). Manual attribution from subjects, used for the rest of this retro:

| Story | Commits (range order, oldest last in `git log`) |
| --- | --- |
| 1-1 | `9050b50` scaffold; `ac32348` review gaps; `a3273af` merge PR #1 (`stories: []`); `1bb8fc2` record 1.1 done (after 1.2 had already started) |
| 1-2 | `35a4f03` types; `d4444b5` review gaps; `20bbd81` close story; `3529ed0` merge PR #2 (`stories: ["1-2"]`) |
| 1-3 | `4484884` DesiredState/Plan/Action; `2057016` review findings; `389d571` merge PR #3 (`stories: ["1-3"]`); `bcc2e48` record 1.3 done |
| 1-4 | `8a14d8d` wire contract; `8130f81` review findings |

Jobs/store remainder (declared in `epics.md` 1.3 ACs, cut from the 1.3 story spec) is **outside this range:** `0fb6869` and `9fc22a6`, after Epic 2.

### Change-volume ranking (`files`, non-merge)

Top production paths by net (integers; `binary_revisions` 0 on every path):

| net | added | deleted | commits | path |
| ---: | ---: | ---: | ---: | --- |
| +1354 | 1355 | 1 | 3 | `Cargo.lock` |
| +1007 | 1035 | 28 | 2 | `crates/core/src/pathschema.rs` |
| +829 | 869 | 40 | 2 | `crates/core/src/walker.rs` |
| +682 | 724 | 42 | 2 | `crates/core/src/install.rs` |
| +624 | 625 | 1 | 3 | `crates/proto/src/lib.rs` |
| +604 | 653 | 49 | 6 | `crates/arch-tests/src/lib.rs` |
| +394 | 406 | 12 | 2 | `crates/core/src/plan.rs` |
| +365 | 366 | 1 | 2 | `crates/core/src/title_id.rs` |
| +274 | 279 | 5 | 2 | `crates/core/src/desired_state.rs` |
| +171 | 178 | 7 | 2 | `bins/mediaops/src/main.rs` |
| +171 | 178 | 7 | 2 | `bins/mediaopsd/src/main.rs` |

## Findings

Nested `bmad-review` was unavailable. Aggregate views were computed inline over `9050b50^..8130f81` plus HEAD for "final state" AC checks. Code lenses (adversarial, edge-case, verification-gap) were run inline over that same diff. Overlap across views is kept; dispositions are on the consolidated items.

### Architecture delta

**Checked and clean for layering in the epic range.** Before `9050b50` there was no Cargo workspace. At `8130f81` the Structural Seed exists: virtual workspace members match `ARCHITECTURE-SPINE.md:246–267` and `Cargo.toml` workspace members today. Adapter crates (`store`, `net`, `ssh`, `arr`, `transfer`, `sync`, `encode`) were empty `lib.rs` skeletons (`git show 8130f81:crates/store/src/lib.rs` is empty; `crates/store/Cargo.toml` has no rusqlite). `mediaops-core` at close depends on blake3/serde/serde_json/thiserror/toml only (`git show 8130f81:crates/core/Cargo.toml`) — no tokio, rusqlite, reqwest, tonic. AD-2 allowlist in `crates/arch-tests/src/lib.rs:9–33` already listed future legal edges (bins → proto, daemon → arr/net); empty extra edges are the subgraph rule. Live test `live_workspace_is_ad2_subgraph` still passes at HEAD. `bins/mediaopsd/Cargo.toml` still has no `mediaops-store` or `mediaops-encode`. No cycle or illegal crate edge was introduced in the range. Banned names (`rsync`/`rclone`/`ftp`/`ssh2`/`russh`/`ffmpeg-next`/`native-tls`) are still enforced (`crates/arch-tests/src/lib.rs:35–43` plus fixture tests).

Later epics filled allowed edges (daemon → net/arr; CLI → store/proto/sync/…). That is out of epic-1 range and still an AD-2 subgraph.

### Duplication map

**F1. Twin composition-root binaries at epic close.** At `8130f81`, `bins/mediaops/src/main.rs` and `bins/mediaopsd/src/main.rs` differ by one line (`const BIN_NAME`). `git diff --no-index` of those two blobs: `1 file changed, 1 insertion(+), 1 deletion(-)`. Matching CLI test files each net +166 in the range. Spec-1-1 residual already named this (`spec-1-1-…md:167` "Duplicate CLI `main`/tests across the two bins"). HEAD has diverged (`mediaops` 993 lines, `mediaopsd` 465) as roles specialized.

- Source: `8130f81` blobs; git_evidence nets +171/+171; spec-1-1 residual.
- Disposition: **accept as-is** (later specialization). Lesson: a shared `bins` helper was not worth a crate at identity-only; copy-paste was cheaper than a premature abstraction.

### God-class / size growth

**F2. `pathschema.rs` was born large, not grown across nine sessions.** Net +1007 in **two** commits (`35a4f03`, `d4444b5`). `walker.rs` +829 and `install.rs` +682 in the same two. Current sizes: pathschema 1107, walker 1042, install 833. Story 1.1 is already marked `warnings: [oversized]`; story 1.2 packed TitleId + PathSchema + walker + install-gate into one session. `arch-tests` +604 across **six** commits is test-law accretion (AD-2, core-IO, ADV-8), not a production god-class.

- Source: git_evidence `files`; `wc -l` at HEAD vs `8130f81`; spec-1-1 frontmatter `warnings: [oversized]`.
- Disposition: **accept as-is** for this epic. Lesson: 1.2 was four subsystems; later epics should not re-bundle that much into one story. Not a fix-now split — HEAD still has one renderer/parser, which is the AD-13 law.

### Pattern divergence

**F3. Commit subjects omit story keys, so `git_evidence.py` cannot attribute 1-1 or 1-4.** Supplied ids were `1-1,1-2,1-3,1-4`. Only merge subjects containing `1-2` / `1-3` populated `commits[].stories`. 1.4 subjects say `story 1.4` (period). PR #1 subject is `cursor/cli-json-and-review-patches` (`stories: []`).

- Source: git_evidence JSON `commits` array; `scripts/git_evidence.py` story matching.
- Disposition: **fix now** (process). Action item 2.

Otherwise conventions held: `thiserror` in libraries / `anyhow` in binaries; no `process::exit` anywhere in `*.rs`; `ExitCode` mapped in the binaries; `CORE_IO_MODULES` carve-out for walker/install enforced by arch-tests (`core_filesystem_io_stays_in_walker_and_install` passes).

**F4. Story 1.1 marked done after 1.2 had started.** `35a4f03` (1.2 types) precedes `1bb8fc2` ("Record 1.1 as done…"). Sprint tracking lagged the code.

- Source: `git log 9050b50^..8130f81`.
- Disposition: **accept as-is** (historical). Lesson: close the sprint row when the story lands, not after the next story's first commit.

### Spec-to-implementation reconciliation

**F5. Epic 1.3 ACs promised `core::jobs` + sqlite; the story spec cut them; they landed after Epic 2.** `epics.md:291–300` requires `advance(state, event)` as the sole state write, repository traits in `core`, `store` as rusqlite adapter, tables `title_index` and `jobs`, and "probes and holds_decisions are not created yet". Spec-1-3 Intent (`spec-1-3-…md:18`) says "Jobs machines and sqlite wait (deferred-work)" and Split remainder (`spec-1-3-…md:81–83`) says they are **not done** and Epic 1 is closed as implemented. At `8130f81` there is no `crates/core/src/jobs.rs` (`git ls-tree 8130f81 crates/core/src/`) and store is an empty skeleton. HEAD now has `advance` (`crates/core/src/jobs.rs:439`), `JobsRepo` (`:578`), `TitleIndexRepo` (`crates/core/src/title_index.rs:72`), store `spawn_blocking` (`crates/store/src/lib.rs:59–71`), `title_index` / `jobs` at `user_version` 2 (`crates/store/src/lib.rs:317–327`) with `title_id` at v3. `probes` (v1) and `holds_decisions` (v6) arrived with later epics, matching the original "land with the stories that write them" clause. Tests: `jobs_advance_is_the_sole_state_write_and_illegal_is_a_repo_error`, `title_index_install_b3_is_immutable_and_replace_updates_current`.

- Source: `epics.md:291–300`; `spec-1-3-…md:18,81–112`; `deferred-work.md:19–31`; commits `0fb6869`, `9fc22a6`.
- Disposition: **accept as-is**. Remainder is implemented; do not reopen 1.3. Record so later retros stop re-flagging the split.

**F6. Staging path in the epic spec / AD-11 still uses colon-form TitleId.** Story 1.2 review changed staging to `TitleId::staging_token()` (`movie-tmdb-603`) because a colon cannot be a directory name on SMB/exFAT/NTFS (`spec-1-2-…md:147–154`; `crates/core/src/pathschema.rs:391–408`; `title_id.rs:129–135`, test `:367–378`). `epics.md:270` still says `_incoming/<TitleId>/…`. `ARCHITECTURE-SPINE.md:135` still says `_incoming/<TitleId serialized>/…`.

- Source: those lines; implementation `staging_path` / `staging_token`.
- Disposition: **fix now** (spec reconciliation, not a code fix). Action item 1.

**F7. "Walker is the sole producer of RemoteRef/RemoteEntry" was amended, not silently dropped.** 1.2 D1 added `from_wire_parts` so proto can `TryFrom` the wire (`spec-1-2-…md:95–96,155–162`; `crates/core/src/walker.rs` constructors). TryFrom impls still live in proto (`crates/proto/src/lib.rs:103–180`). Epic layering law holds; the "sole producer" sentence in `epics.md:265` is now imprecise.

- Disposition: **accept as-is** (story spec change log is the record). Optional: one-line epic-spec note in the same reconciliation as F6.

**F8. `REPACJ` is a public scene tag because the spec lists it.** `SCENE_TAGS` = `["REPACJ", "REPACK", "PROPER"]` (`pathschema.rs:17`). Deferred-work (`deferred-work.md:17`) and spec-1-2 frontmatter still ask whether this is a REPACK typo.

- Disposition: **defer** (already on the 1.2 ledger). Not a new sprint item.

### Adversarial lens (inline over `9050b50^..8130f81`)

Lens requires ≥10 concrete findings. Nested review unavailable; this list is the inline pass. Overlap with aggregate views is noted.

| # | location | trigger_condition | guard_snippet | potential_consequence | disposition |
| --- | --- | --- | --- | --- | --- |
| A1 | `bins/mediaops/src/main.rs` vs `bins/mediaopsd/src/main.rs` at `8130f81` | Twin mains differ by `BIN_NAME` only | Shared identity/envelope helper, or accept role split | Bugfixes had to be applied twice (`ac32348` patched both) | accept as-is (F1) |
| A2 | `epics.md:291–300` vs `spec-1-3-…md:81–83` | Epic AC still requires jobs/store; story closed without them | Keep remainder on deferred-work until landed (it did: `0fb6869`) | Epic marked done while declared ACs were open | accept as-is (F5) |
| A3 | `epics.md:270`; `ARCHITECTURE-SPINE.md:135` | Staging AC still implies colon TitleId as a directory | Reconcile to `TitleId::staging_token()` | Operators/docs render a path that cannot exist on SMB/NTFS | **fix now** (F6) |
| A4 | `crates/core/src/pathschema.rs:17` | `REPACJ` frozen as a public scene tag | Confirm upstream or comment that the misspelling is intentional | Wrong tag list, or a real REPACJ release is the only reason it exists | defer (F8) |
| A5 | `crates/core/src/walker.rs:467–507` | `walk_dir` recurses with no depth cap | Depth limit or documented trust boundary | Deep remote tree can exhaust the stack | defer (ledger) |
| A6 | `crates/core/src/pathschema.rs:419–433` | `validate_display_token` has no `NAME_MAX` | Refuse components >255 bytes at render | Failure is opaque `InstallError::Io` at `rename` | defer (ledger) |
| A7 | `crates/core/src/install.rs:277` | `fs::rename` with no parent-dir `fsync` | `File::open(parent)?.sync_all()` after rename | "Atomic" install is not crash-durable | defer (ledger) |
| A8 | `git show 8130f81:crates/core/src/control_port.rs` `async fn df() -> Result<Bytes, …>` | Trait dropped `DfResponse.semver` / `proto_package` | Return a snapshot that carries handshake fields | AD-22 handshake unusable through the port (later fixed: `DfSnapshot` at `control_port.rs:57–61,17`) | accept as-is (later fixed) |
| A9 | `crates/arch-tests/src/lib.rs:239–243` | ADV-8 detector is substring `contains` | rust-analyzer / syn scan, or accept residual | Aliases/macros can hide `Status` constructors | defer (1.4 residual) |
| A10 | git_evidence `commits[].stories` | Subjects omit `1-1` / `1-4` | Require story keys in commit subjects | Epic retro cannot attribute commits mechanically | **fix now** (F3) |
| A11 | spec-1-1 and spec-1-4 frontmatter `followup_review_recommended: true` | Flags still true after later epics consumed the crates | Set false or queue the follow-up | Review debt looks open when the stories are `done` | **fix now** (process) |
| A12 | `crates/core/src/walker.rs:84,553–562`; proto `RemoteEntry` | `nlink` without `dev`/`ino`; non-unix returns `1` | Wire change, or document unix-only | Hardlinks cannot be paired; non-unix always "not linked" | accept as-is (wire frozen; no non-unix target) |
| A13 | `crates/proto/src/lib.rs:479–487,516–524` | Decode trusts `exit_code` and ignores `reason` | Re-check `reason == exit_code.error_code()` or drop reason | Tampered/mismatched reason survives parse | defer (1.4 residual) |
| A14 | spec-1-1 residual; `bins/mediaops/src/main.rs:957` | `--help --json` is clap help (exit 0), not an envelope | Document as clap contract (already tested: `json_token_requests_json("--help")` is false) | `--json` users parsing stdout for `{ok}` break on `--help` | accept as-is |

### Edge-case lens (inline)

Unhandled paths in the epic-1 diff that still lack a guard. Handled 1.2/1.3/1.4 review patches (non-ASCII panic, proto3 `DELETED=0`, digest-before-parse, nested allowlist, etc.) are discarded.

| # | location | trigger_condition | guard_snippet | potential_consequence | disposition |
| --- | --- | --- | --- | --- | --- |
| E1 | `walker.rs:467–507` | Recursion depth unbounded | `if rel.components().count() > N { skip }` | Stack overflow on a deep allowlisted tree | defer (A5) |
| E2 | `pathschema.rs:419` | Rendered component longer than 255 bytes | `if token.len() > 255 { PathSchemaError }` | Install fails as generic IO | defer (A6) |
| E3 | `walker.rs:553–562` | `#[cfg(not(unix))]` `nlink = 1` | `cfg`-gate tests or document unix-only | Hardlink detection always false off unix | accept as-is (A12) |
| E4 | `install.rs:277` | Rename without parent `fsync` | Parent-dir sync after rename | Lost install on power loss | defer (A7) |
| E5 | `from_wire_parts` (walker) | Wire-borne ref cannot re-stat the remote FS | Keep validating constructor only | Escape paths that pass string checks | accept as-is (F7; 1.2 D1) |

Deletion check: the range is additive (greenfield workspace). No retired production contract. Empty deletion set.

### Verification-gap lens (inline)

Changed behaviors in the epic-1 diff, traced to consumers. Tests read before claiming coverage.

**V1. `ControlPortClient` was type-checked, not RPC-exercised, in 1.4.** Spec-1-4 residual (`spec-1-4-…md:169`). Proto tests at close covered conversions (`error_detail_round_trips_through_status_details`, `remote_ref_happy`, …) not a live client. Demonstration: a broken `df` mapping would not fail `cargo test -p mediaops-proto` at `8130f81`. Later consumers exist (`bins/mediaops/src/run.rs:122`, `crates/net/src/lib.rs:367`) with Epic 2+ tests. **accept as-is** for this epic (1.4 Never list: no live serve). `gap_shape: regression-gap` at close; closed by later adoption.

**V2. ADV-8 substring scan.** `constructs_tonic_status` (`arch-tests/src/lib.rs:239–243`). Tests `status_construction_outside_proto_is_detected` and `tonic_status_is_constructed_only_in_proto` pass for fully-qualified and imported constructors they fixture; an alias would not fail. **defer** (A9). `gap_shape: broken-verification-gap`. Consumer: any crate outside `crates/proto`.

**V3. `ErrorDetail.reason` is not a decode invariant.** `error_detail_from_status` (`proto/src/lib.rs:516–524`) checks `exit_code` only; `TryFrom<ErrorDetail> for ControlError` (`:479–487`) copies `message` and maps `exit_code`. Encode always sets `reason` from `error_code()` (`:469–476`). A mismatched reason would not fail those tests (they assert encode-side equality, e.g. historical `8130f81` proto tests around reason). **defer**. `gap_shape: regression-gap`.

**V4. `--help --json` envelope.** 1.1 I/O matrix is `--json` / human / unknown flag (`spec-1-1-…md:41–46`). `--help` is clap, exit 0 (`bins/mediaops/tests/cli.rs:107`). Residual documented. **accept as-is**.

**V5. TTY (human) tracing is not pty-tested.** spec-1-1 residual (`spec-1-1-…md:167`). Piped tests assert JSON tracing lines (`cli.rs:13–24`). A regression in the tty branch would not fail CI. **accept as-is** (no pty in default suite; AD-20).

**V6. jobs/store verification did not exist at epic close.** No `jobs.rs` at `8130f81`. HEAD has 136 core tests including `jobs::*` and 20 store tests. **accept as-is** (F5).

Non-behavioral parts of the range (sprint-status edits, spec markdown) were not treated as verification gaps.

## Behavior verification

Epic 1 is types + offline tests. No live SeedIt4Me box and no GPU were used.

Ran on this machine (2026-09-02), default features, `--offline --locked`:

| Command | Result |
| --- | --- |
| `rustc --version` | `rustc 1.98.0 (88d9e12ae 2026-08-18)` |
| `cargo test -p mediaops-arch-tests -p mediaops-core -p mediaops-proto -p mediaops-store --offline --locked` | arch-tests **14** passed; core **136** passed; proto **21** passed; store **20** passed; 0 failed, 0 ignored |
| `cargo test -p mediaops -p mediaopsd --offline --locked --test cli` | mediaops CLI **23** passed; mediaopsd CLI **12** passed (1.1 JSON/human/usage matrix still present: `json_happy`, `human_happy`, `usage_unknown_flag`, `usage_unknown_flag_with_json`, `usage_json_equals_false_stays_human`) |

`bins/mediaops/Cargo.toml` has `default = []` and `live-box = []` — live-box is not on the default path. CI at `8130f81` and HEAD is `cargo test --locked --offline --workspace` after `protobuf-compiler` (`.github/workflows/ci.yml`).

Not exercised: any RPC against a running `mediaopsd`, mTLS, sqlite on the operator box, NVENC, or the SeedIt4Me host. Passing unit tests do not substitute for those flows; those flows are later epics.

## Previous-retro follow-through

There was nothing to follow through on, for both reasons the rubric distinguishes:

1. **No prior retro document** — no `epic-*-retro-*.md` under `{implementation_artifacts}` (batch start).
2. **No `action_items` in `sprint-status.yaml`** — the file defines action-item statuses in comments (`sprint-status.yaml:19–22,30`) and has no `action_items:` array.

No `--set-action-status` transitions would have been proposed. Headless: the flag is not passed.

## Action items

Proposed, not applied. This run does not edit `sprint-status.yaml` (parent serializes status writes).

1. **Reconcile staging-path wording** to `_incoming/<TitleId::staging_token()>/` (hyphen form, e.g. `movie-tmdb-603`) in `epics.md` Story 1.2 and `ARCHITECTURE-SPINE.md` AD-11. Evidence: F6. Kind: spec reconciliation. Owner: **Coto**.
2. **Put story keys in commit subjects** (`1-1`, `1-2`, `1-4` not `story 1.4`) so `git_evidence.py --stories` can attribute. Evidence: F3/A10. Kind: process lesson. Owner: **Coto**.
3. **Resolve stale `followup_review_recommended: true`** on `spec-1-1-workspace-scaffold-and-dependency-law.md` and `spec-1-4-wire-contract-in-the-proto-crate.md` (clear now that later epics consumed those crates, or queue the follow-up). Evidence: A11. Kind: process. Owner: **Coto**.

Deferred items that stay on `deferred-work.md` (not duplicated as new sprint actions): NAME_MAX, walker depth, nlink/`dev`/`ino`, non-unix nlink, Proper/Repack titles, parent-dir fsync, REPACJ confirmation, ADV-8 substring scan, `ErrorDetail.reason` decode.

## Acceptance verdict

**accepted-with-open-items** (machine verdict; no human override in this headless run). **Criteria: declared.**

`pending_stories` is empty, so unfinished-story rejection does not apply.

Declared ACs vs **final state (HEAD)**:

| Story | AC (from `epics.md`) | Evidence at HEAD |
| --- | --- | --- |
| 1.1 tree | Structural Seed paths, Rust 1.98 / edition 2024, pinned tonic 0.14.6 / prost 0.14.4 / rusqlite 0.40.2 / … | `rust-toolchain.toml`; `Cargo.toml` members + pins; `rustc 1.98.0` |
| 1.1 AD-2 | arch-tests fail illegal edges | 14 arch-tests passed, including live subgraph + reqwest/rusqlite/encode-in-daemon/banned-name fixtures |
| 1.1 binaries | composition roots; ExitCode 0–5; `{ok,data,error}` stdout; tracing stderr | `ExitCode` `lib.rs:75–82`; CLI `json_happy` / `human_happy` / usage tests passed |
| 1.1 tests | default suite needs no network/box/GPU; `CapabilityToken` no LLM | `live-box` opt-in; `CapabilityToken` `lib.rs:176–182`; core Cargo.toml has no LLM crate |
| 1.2 identity | `parse(render(id)) == id`; year in folder and file; spaces refused; remasters by MBID | `pathschema::tests::pathschema_parse_render_identity_movie_series_album`, `library_year_matches_folder_and_file_stem`, `spaces_in_title_are_refused`, `remaster_folders_share_album_title_id` |
| 1.2 scene/reject | REPACJ/REPACK/PROPER stripped; `needs-split` / `needs-year` | `scene_tag_strip_removes_repacj_repack_proper`; `reject_bins_needs_split_and_needs_year` |
| 1.2 walker | typed `RemoteRef`/`RemoteEntry`; unknown errors; no symlink escape; torrents skipped | walker tests listed in the 136 core results |
| 1.2 staging/gate | `staging_path`; `install` / `replace` | `pathschema.rs:395`; `install.rs` `install`/`replace`; hyphen token is the accepted 1.2 spec change (F6) |
| 1.3 DesiredState | `deny_unknown_fields`, required `schema_version`, unit-suffixed sizes → `Bytes` | `desired_state.rs:21–27,287–293,328–381`; tests `ds_happy_sizes_are_bytes`, `unknown_field_is_denied`, `missing_schema_version_fails` |
| 1.3 Plan/Action | raw TOML bytes + blake3; exhaustive Action | `plan.rs:77–114,18–45,264–293` |
| 1.3 jobs/store | `advance`; traits in core; `title_index`/`jobs`; daemon store-free | HEAD: `jobs.rs:439,578`; store v2+ DDL; mediaopsd Cargo.toml has no store. **Late vs epic close; complete now (F5).** |
| 1.4 proto | `tonic-prost-build`; sole From/TryFrom; field-for-field refs; two Status fns; ControlPort in proto; additive `mediaops.v1` | `crates/proto/build.rs`; `proto/src/lib.rs:1–8,493–524,537–568`; `proto/mediaops.proto:1–16`; later RPCs are additive |

Open items that keep this off plain **accepted**: F6 (spec still lies about staging directories), the 1.2 deferral ledger (A4–A7, A12, Proper/Repack), A9/A13 (1.4 residuals), and A11 (stale follow-up flags). None of those block the declared ACs at HEAD.

## Open questions

- Is `REPACJ` a real scene tag, or a `REPACK` typo that should be dropped from `SCENE_TAGS`? A human product call; the implementation is faithful to the written spec (`epics.md:258–260`, `pathschema.rs:17`).
- Should NAME_MAX and walker depth be required before a large allowlisted remote is walked in production, or is "roots are trusted" the v1 rule? Already on `deferred-work.md`.
- Should AD-11's "atomic rename" language require parent-dir `fsync`, or is crash-durability an Epic 3/4 concern?
- Spec-1-1 and spec-1-4 still recommend a follow-up review. Is that still wanted now that Epics 2–6 have consumed those crates?

## Assumptions

Headless run; every choice made without the user:

- **Epic selected:** invocation Epic 1 ("The laws live in types"), not auto-detect. `detect-epic --epic 1` confirmed `story_count: 4`, `pending_stories: []`, `retro_key: epic-1-retrospective`, `retro_status: optional`. (The script's `done_stories` array also lists later epics' done keys; this retro scoped only to Epic 1's four stories.)
- **Completeness:** proceeded with empty `pending_stories`. Machine unfinished-story reject does not apply.
- **Diff range:** `9050b50^..8130f81` as confirmed by `git log`. Remainder commits `0fb6869` / `9fc22a6` were read for final-state AC checks, not folded into git_evidence `files`.
- **Verdict:** machine **accepted-with-open-items** on declared ACs at HEAD, with named deferred/spec items. No human override.
- **Proposed items:** the three action items above (staging spec recon; commit-subject keys; stale follow-up flags). Not applied.
- **`project-context.md`:** absent; continued.
- **Activation prepend/append / on_complete:** empty.
- **Phase 3:** skipped (headless; never ask the user).
- **Nested subagents:** unavailable. Aggregate views and `bmad-review` lenses (adversarial, edge-case, verification-gap) ran inline over this epic's diff. Narrowing recorded here.
- **Session logs:** none for 1.1–1.4; process-lesson analysis that needs them was skipped.
- **Previous retro / sprint `action_items`:** neither exists; follow-through is the explicit "nothing to follow through on" record above. No `--set-action-status`.
- **Sprint-status write:** not performed. This run writes only this document.
- **Live behavior:** not exercised (no SeedIt4Me box, no GPU), per epic scope and parent constraint.
- **Criteria class:** declared (`epics.md` GWT ACs exist), not profiled.
