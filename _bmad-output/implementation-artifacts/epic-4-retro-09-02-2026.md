---
epic: 4
date: 09-02-2026
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 4 retrospective — Tonight-playable on this box

## Epic summary

Epic 4 (`Tonight-playable on this box`) from `_bmad-output/planning-artifacts/epics.md`. Sprint mode. `detect-epic --epic 4` (pre-run): `story_count: 3`, `pending_stories: []`, `retro_key: epic-4-retrospective`, `retro_status: optional`.

**Diff range:** `ee76a85^..279f166` (`5813ab1..279f166`). Confirmed first story commit `ee76a85`, last epic commit merge `279f166` (PR #5). `git_evidence.py --stories 4-1,4-2,4-3` returned `commit_count: 6`, `merge_count: 1`, `merges_measured: 1`, `binary_revisions: 0` on every path. Every commit's `stories` array was empty: subjects do not contain word-boundary `4-1` / `4-2` / `4-3` (`aac0d70` says `4.1` with a dot). Per-story attribution below is from handoff commit messages, not from the script.

| SHA | Subject | Story (manual) | `is_merge` |
| --- | --- | --- | --- |
| `ee76a85` | Add plan/run/watch/why/status and the grabber=None planner. | 4.1 | no |
| `e8413c9` | Execute EncodePolicy on the home GPU with reversible replace. | 4.2 | no |
| `cc26fe4` | Add the first-demo runbook and the live-box test gate. | 4.3 | no |
| `aac0d70` | Close 4.1 review findings and add Makefile/README. | 4.1 follow-up | no |
| `3900173` | chore: update gitignore | unattributed | no |
| `279f166` | Merge pull request #5 from cotocisternas/epic/4-tonight-playable | merge | yes |

Highest non-merge churn (`files`; do not add `merge_files`): `crates/sync/src/apply.rs` +856/−13, `crates/sync/src/plan.rs` +549/−10, `bins/mediaops/src/encode_cmd.rs` +446/−0, `bins/mediaops/src/run.rs` +408/−33, `epic-4-handoff.md` +385/−0, `bins/mediaops/src/status.rs` +338/−42, `crates/encode/src/run.rs` +282/−0, `bins/mediaops/tests/cli.rs` +270/−11, `bins/mediaops/src/main.rs` +234/−7, `crates/encode/src/ffprobe.rs` +217/−0.

**Stories completed (sprint-status):** `4-1-sync-plan-apply-and-cli-verbs`, `4-2-encode`, `4-3-first-demo-on-this-box`, `epic-4: done`. No unfinished keys.

**Criteria:** declared in `epics.md` Epic 4 (stories 4.1–4.3) plus FR1, FR2, FR3, FR6 (pull/encode/lock/watermark slice), FR15 (Copy/Skip/Encode apply), FR16, FR19, FR20, FR25, NFR7, NFR12. Implementation session used `_bmad-output/implementation-artifacts/epic-4-handoff.md` as the locked contract (D1–D9).

**Evidence inventory**

| Item | Status |
| --- | --- |
| Epic spec | Present: `epics.md` `## Epic 4` (declared ACs). |
| Story files | **Missing.** No `spec-4-*` under `{implementation_artifacts}`. Intent is the handoff + `epics.md`. |
| Diff + git_evidence | Present. Range above. Story-id match **narrowed** (empty `stories` arrays). |
| Sprint status | Present. `epic-4-retrospective: optional`. No `action_items` key (comment only). |
| Previous retro | **Missing.** No `epic-*-retro-*.md`. Batch start. |
| Session logs | **Missing.** No conversation/session records for 4.1–4.3. Process-lesson-from-logs **skipped**. |
| Handoff / runbook | Present: `epic-4-handoff.md`, `demo-epic-4.md`. |
| Existing review | Present: `review-pr-5-epic-4.md` — 4.1 only, all `[x] [Review][Patch]`. Chunks 4.2 and 4.3 never written. |
| Architecture / PRD | Spine ADs 4, 6, 9, 10, 16–18, 20; `increments.md` first-demo list. |
| `project-context.md` | **Absent** (`file:{project-root}/**/project-context.md`). |
| Post-range commits | `e1d9b6b` (test coverage on encode/run/status), then Epic 5/6 PRs, also touch `crates/sync` / CLI. Line numbers below are **HEAD**. Swallow of `after_install` originates in `e8413c9` and remains. |

## Findings

### Spec-to-implementation reconciliation

1. **Live first demo was never run.** Source: `epics.md` Story 4.3 first AC (“When the demo runs against the live SeedIt4Me box and home GPU”); `demo-epic-4.md:3` and `:47` (“live execution pending operator confirm”); `epic-4-handoff.md` D8 / 4.3 deliverable 4 (“4.3 can be `done` when the runbook and gate exist even if the live run has not been executed”); `bins/mediaops/tests/live.rs` (`#[ignore]`, returns without SSH/encode even if `MEDIAOPS_LIVE=1`). **Disposition:** defer (open item). **Upstream:** keep the split explicit — story-done ≠ live proof. Do not treat the runbook as a completed demo.

2. **Plan does not emit `Action::Encode`; apply no-ops it.** Source: `crates/sync/src/plan.rs` `plan_actions` (Copy/Skip/GrabApply/EdgeApply only); `crates/sync/src/apply.rs:128-132` (`Action::Encode { .. } => {}`); encode happens in `bins/mediaops/src/run.rs:169-188` via `encode_cmd::after_install`. Handoff D7: “Plan artifact is not a perfect encode preview until after pull… Apply-after-install is enough.” **Disposition:** accept as-is (D7). **Upstream:** none for this instance; later retros should not re-flag Encode-in-plan as a defect against Epic 4.

3. **NVENC stored cap stayed encoder presence 0/1.** Source: `crates/encode/src/lib.rs` `probe_nvenc` (`cap: u32::from(hevc)`); handoff D7/D9; `deferred-work.md` previously said a real session cap “waits for Epic 4 encode”. Epic 4 kept 0/1 on purpose. **Disposition:** accept as-is.

4. **`encode scan` never reports series-skip rows.** Source: `bins/mediaops/src/encode_cmd.rs:61-68` (Movie filter); test `scan_empty_and_movies_only_with_canned_probe` asserts `files.len() == 1` with a series file on disk; series-skip itself is `crates/encode/src/policy.rs:55-59` + `series_hevc_mp4_is_keep_named_series_skip`. Handoff: “classify files under `movies/` (report series-skip)”. **Disposition:** accept as-is (scan scope is movies/; the named rule lives in `classify`).

### Architecture delta

Computed inline from `git diff ee76a85^ 279f166 -- '**/Cargo.toml'` plus import grep (no pydeps/dependency-cruiser). **Narrowed:** graph-before/after tooling not run.

- `mediaops-sync` gained `mediaops-transfer` (handoff D2 allowed). No `mediaops-net` in `crates/sync`.
- `mediaops-encode` gained serde for ffprobe JSON. Still linked only from `bins/mediaops` (`bins/mediaopsd/Cargo.toml` has no encode/store). `cargo test -p mediaops-arch-tests` green (`encode_in_mediaopsd_closure_is_violation` still in the suite).
- `bins/mediaops` gained empty feature `live-box` (D8).
- CLI `plan`/`run`/`watch`/`why`/`status`/`encode` talk to the home UDS (`connect_home`); no `seedbox_address` / seedbox dial in `bins/mediaops/src` (grep empty). Home unit `ExecStart=` is `mediaopsd serve --role home` (`bins/mediaops/tests/cli.rs:350-356`).

No new crate-graph violation found. Not a defect.

### Duplication map

5. **Three ffprobe-failure policies in one CLI crate.** Source: `encode_cmd.rs:69-71` scan `Err(_) => EncodeDecision::Keep`; `encode_cmd.rs:312-314` `after_install` `Err(_) => return Ok(())`; `encode_cmd.rs:167-169` `encode run TITLE` maps probe errors to `AppError::Runtime`. **Disposition:** fix now (same action as finding 9). **Upstream:** one probe-error contract for scan / after-install / encode-run.

6. **`placement_filename` re-implements `render(…).file_name()`.** Source: `encode_cmd.rs:359-375` discards `encode_to_converting`’s path (`let _ = converting`) then rebuilds via `placement_filename`. **Disposition:** defer. Divergence would send `replace` at a missing `.converting`.

7. **Lock skip implemented twice.** Planner emits `Skip { reason: lock }` (`plan.rs:175-181`); apply also skips `Copy` when `ds.lock()` (`apply.rs:90-93`). Defense in depth after snapshot match. **Disposition:** accept as-is.

### God-class / size growth

Ranked from `git_evidence.py` `files`, then opened current sizes. Production logic is not a god class:

| Path | Epic net | HEAD lines | Notes |
| --- | --- | --- | --- |
| `crates/sync/src/apply.rs` | +843 | 1033 | Apply loop ~lines 1–345; rest tests. |
| `crates/sync/src/plan.rs` | +539 | 864 | `plan_actions` ~44–250; rest tests. |
| `bins/mediaops/src/encode_cmd.rs` | +446 | 737 | Commands ~1–449; rest tests (`e1d9b6b` grew tests after the merge). |
| `bins/mediaops/src/run.rs` | +375 | 680 | Replaced the Epic 4 stub. |
| `bins/mediaops/src/bootstrap.rs` | +98 | 1106 | Pre-existing large file; epic added lock-holder/truncate fix. |
| `crates/core/src/pathschema.rs` | +69 | 1107 | Pre-existing; `parse_placement` added. |

**Disposition:** no god-class finding. `merge_files` restates the same paths (one first-parent merge, fully measured).

### Pattern divergence

Covered by finding 5 (probe-failure). Crates use `thiserror`, bins `anyhow`, as required. Exclusive vs lock-free verbs match handoff D5 (`watch`/`why`/`status`/`encode pause` have no `exclusive_lock`; `plan`/`run`/`encode run`/`library bootstrap` do).

### Existing 4.1 review — re-check

`review-pr-5-epic-4.md` lists 24 4.1 items, all marked patched in `aac0d70`. Spot-check against HEAD (not re-litigated as new findings):

- Duplicate TitleId → `Skip { duplicate_title }`: `plan.rs:84-88`.
- On-disk schema as installed: `run.rs:260-264` + `scan_schema_files`.
- Lock truncate: `bootstrap.rs:669-687` `truncate(false)`, rewrite only after lock.
- `min_free_gib = 0` vs file larger than free: `plan.rs:212-213` + `min_free_zero_does_not_copy_a_file_larger_than_free_disk`.
- `why` prefers open Want: `status.rs:73-83`.
- Satisfy Want after install / `upgrade_never`: `apply.rs:110-115`, `242-246`.
- `record_install` after `PullEvent::Install`: `apply.rs:234-241` (order reversed from the original bug; leftover gap is finding 12).
- `JobView.title_id`, watermark, `stat` present: `status.rs`.
- `run` applies the JSON artifact: `run.rs:127-128` `Plan::from_json_slice`.
- Unique open Want: `store/src/jobs.rs:188-192` + `create_want_is_idempotent_for_one_open_row`.
- `pull_matches` when intent exists: `apply.rs:183-191` + `in_flight_pull_with_different_remote_is_refused`. Missing-intent path is finding 11.
- `placement_for` TitleId mismatch is usage: `home.rs:318-324`.
- Unique plan path: `run.rs:373-397`.
- Home unit `ExecStart=`: `cli.rs:350-356` (not Description-only).

**4.2 and 4.3 were never in that review file** (“Remaining chunks: 4.2 encode, 4.3 runbook/live-box”). Process lesson, not a code defect.

### Diff-scope review (inline `bmad-review`)

**Narrowing:** `bmad-review` not spawned (parent forbids nested subagents). Lenses **adversarial**, **edge-case-hunter**, **verification-gap** run inline over `ee76a85^..279f166`, with HEAD as the as-built source. Editorial structure/prose lenses not run (content class: code + runbook). `also_consider`: story boundaries 4.1 apply ↔ 4.2 `after_install`, 4.3 runbook ↔ live AC.

#### Adversarial

8. **`mediaops run` discards encode errors.** Location: `bins/mediaops/src/run.rs:176-188`. Trigger: `let _ = encode_cmd::after_install(…).await`. Guard: propagate or put `{ok, ran, skipped, error}` on `RunData`; do not delete the plan file until encode outcome is recorded. Consequence: demo path “pull → install → encode” (`handoff` D7, `demo-epic-4.md` step 7) can leave HEVC-MP4 unplayable while `run` prints copies success (exit 0). Present since `e8413c9`. **Disposition:** fix now.

9. **ffprobe failure is treated as “do not encode”.** Location: `encode_cmd.rs:69-71`, `312-314`. Trigger: scan classifies Keep; `after_install` returns `Ok(())`. Guard: fail scan/after-install (or emit `decision: "probe_error"`) instead of Keep/skip. Consequence: Chrome-breaking HEVC-MP4 stays HEVC when ffprobe is missing. **Disposition:** fix now.

10. **`encode run TITLE` never writes an Encode job.** Location: `encode_cmd.rs:161-230`. Trigger: one-shot transcode + `record_replace` only. Guard: create/advance Encode like `after_install`. Consequence: `why`/`status` encode-queue stays empty for the verb the runbook uses after a refused-inside-run path. **Disposition:** defer.

11. **In-flight Pull without `pull-intent.json` is reused.** Location: `apply.rs:338-344` `None => Ok(true)`. Trigger: crash after `Start` before `write_pull_intent`. Guard: refuse reuse when intent is missing and a `.partial` exists, or always write intent before `Start`. Consequence: a later Copy with a different remote can resume the wrong bytes. Test only covers *present* mismatched intent. **Disposition:** defer.

12. **`record_install` after `Install` can leave Installed without an index row.** Location: `apply.rs:234-241`. Trigger: `advance(Install)` then `titles.record_install` fails. Guard: record then advance, or roll the job back. Consequence: next plan may rely on `on_disk` scan; Want stays Open until `upgrade_never` skip. **Disposition:** defer (inverse of the patched 4.1 order).

13. **`replace` refuses an existing backup.** Location: `crates/core/src/install.rs:315-317`, `backup_under_ops_is_legal_and_movies_is_not`. Trigger: second `replace` to the same `_ops/backup-hevc-originals/<token>/<file>`. Guard: unique backup name or refuse with a typed encode error. Consequence: a retry after a partial success cannot replace. First successful encode then Keep (H.264) avoids the path. **Disposition:** defer.

14. **`probe_media` always execs program `"ffprobe"`.** Location: `crates/encode/src/ffprobe.rs:26-28`. Trigger: `ffmpeg_path` in machine kv is used for ffmpeg only. Guard: derive ffprobe from `ffmpeg_path` or store `ffprobe_path`. Consequence: custom ffmpeg location, ffprobe not on `PATH`, encode skipped/errored depending on finding 9. **Disposition:** defer.

15. **`watermark_view` hides desired-state errors.** Location: `status.rs:202-214`. Trigger: unreadable/invalid TOML → `min_free: null`. Guard: surface parse failure on `why`/`status`. Consequence: FR6 watermark slice looks empty instead of broken. **Disposition:** defer.

16. **`RunData` has no encode fields.** Location: `run.rs:27-33`, `192-201`. Trigger: JSON `run` reports copies/skips/installed only. Guard: include encode ran/skipped/error. Overlaps finding 8. **Disposition:** fix now (same item).

Adversarial count on this pass: 10 concrete issues (8–16 plus live-demo #1). Empty list was not used.

#### Edge-case hunter

Unhandled paths (handled ones omitted):

17. **Missing intent + existing `.partial` (finding 11).** `apply.rs:207-220` Pulling branch writes intent then `pull_file`. **Disposition:** defer.

18. **`encode_to_converting` ExecPort `Err` vs nonzero status.** Location: `crates/encode/src/run.rs:72-79`. Nonzero status unlinks `.converting`; a transport `Err` after a partial write does not. **Disposition:** defer.

19. **`scan_schema_files` is one title-folder deep.** Location: `crates/sync/src/lib.rs:107-133`. Nested `extra/` is skipped (tested). Files loose under `movies/` are omitted. Planner also requires `parse_placement`. **Disposition:** accept as-is.

No deletion finding: the Epic 3 stub `"plan/apply waits for Epic 4"` is gone from HEAD (present on `ee76a85^` in `bins/mediaops/src/run.rs`, `crates/encode/src/lib.rs`, `crates/sync/src/lib.rs`) and replaced by real plan/apply/encode.

#### Verification-gap

20. **Regression gap — `after_install` on `cmd_run`.** Location: `run.rs:176-188`. Consumer: `cmd_run` after Copy install. Demonstration: drop the `after_install` call or make it always `Err`. Tests read: `cmd_run_copies_through_home_socket_and_installs` (`run.rs:498-534`) asserts copies=1 and title-index path, not encode/current_b3; no other test references `after_install` (repo grep: definition + one call). `let _ =` means even a failing encode would not fail that test. **gap_shape:** `regression-gap`. **Disposition:** fix now (with finding 8).

21. **Regression gap — scan probe failure → Keep.** Location: `encode_cmd.rs:69-71`. Tests read: `scan_keep_and_refuse_from_probe` uses successful H264/HDR JSON; `scan_empty_and_movies_only_with_canned_probe` uses successful HEVC10. No test with `ExecPort` error. **gap_shape:** `regression-gap`. **Disposition:** fix now (with finding 9).

22. **Broken-verification — live-box gate does not exercise the demo.** Location: `bins/mediaops/tests/live.rs`. Consumer: Story 4.3 live AC. Test is `#[ignore]`, returns immediately if `MEDIAOPS_LIVE != 1`, and still does not SSH/encode if it is. Intentional per D8; it does not verify the live AC. **gap_shape:** `broken-verification-gap`. **Disposition:** accept as-is for CI; live proof remains finding 1.

`Action::Encode` apply being a no-op is **not** a missing-adoption gap: D7 is a supersession signal that after-install replaces Encode-in-plan.

## Behavior verification

Runtime on this box was **offline only**. Live SeedIt4Me, WAN pull, and Ada GPU NVENC were **not** exercised (handoff + runbook + parent: do not touch the live box).

| Command | Result |
| --- | --- |
| `cargo test --workspace --offline` | ok. Non-doc tests: mediaops 62 + cli 23 + live (no feature) 0; arch-tests 14; arr 67; core 136; encode 18; net 23; proto 21; ssh 11; store 20; sync 38; transfer 17; mediaopsd 7 + cli 12. 0 failed. |
| `cargo test -p mediaops-arch-tests` | included above, 14 passed (AD-2: encode not in mediaopsd). |
| `cargo test -p mediaops --features live-box --offline --test live -- --list` | `live_demo_pending_operator_confirm` listed; default suite does not run it. |
| `cargo run -p mediaops -- --help` / `run --help` / `encode --help` | verbs `plan`, `run`, `watch`, `why`, `status`, `encode scan\|run\|pause` present. |

Named tests cited in the handoff that exist and passed: `planner_music_first_then_movie_then_series`, `installed_is_skip_upgrade_never_not_auto_upgrade_1080p_to_4k_remux_because_disk_is_bored`, `watermark_skip_rest_of_class`, `movie_hevc10_mp4_is_nvenc_h264_hevc_mp4_chrome_dropped_frames`, `failed_ffmpeg_leaves_original_in_place_no_delete_before_replace`, `lock_conflict_is_exit_3_never_silent_0`, `partial_resume_still_works_when_apply_is_killed_mid_file`, `backup_under_ops_is_legal_and_movies_is_not`.

Passing tests are not a live demo. The 4.3 success bar (“beat need FTP-PASV”) is **unmeasured**.

## Previous-retro follow-through

Nothing to follow through: no prior retro document under `{implementation_artifacts}/**/epic-*-retro-*.md`, and `sprint-status.yaml` has no `action_items` array (only the workflow comment that retrospectives append one). No `--set-action-status` selectors. Headless: would have proposed none.

## Action items

Proposed, not applied (this run does not edit code or sprint-status).

1. **Surface encode outcome on `mediaops run`.** Stop discarding `after_install` (`run.rs:176-188`); put ran/skipped/error on the JSON envelope; add a test that `cmd_run` of an NvencH264 install records `current_b3` change or fails visibly. Owner: Coto. Remediation (findings 8, 16, 20).
2. **Treat ffprobe failure as an error (or an explicit `probe_error` decision), never Keep/silent skip.** Align scan and `after_install` with `encode run TITLE`. Owner: Coto. Remediation (findings 5, 9, 21).
3. **Run the Epic 4 live demo after operator confirm** using `demo-epic-4.md`; record measured throughput; do not claim FTP-PASV beaten until then. Owner: Coto. Deferred live AC (finding 1).

Process (not a sprint fix): put `4-1` / `4-2` / `4-3` in commit subjects so `git_evidence.py` can attribute; persist 4.2/4.3 review chunks when a PR review is story-chunked.

## Acceptance verdict

**accepted-with-open-items.** Criteria were **declared** (not profiled). `pending_stories` is empty, so unfinished stories do not force rejected.

**Met in evidence (offline):**

- 4.1: pure planner Copy/Skip, music-first, `upgrade_never`, watermark/`max_copy`, `run` = plan then apply of the JSON artifact, snapshot mismatch exit 4, lock exit 3, `watch` lock-free Want, `why`/`status` pull/watermark/lock/encode-queue with local `stat`, `--json` envelope, UDS-only CLI.
- 4.2: EncodePolicy Keep | NvencH264 | Refuse; HEVC-MP4 Chrome named test; series-skip named test; HDR/DV/2160p Refuse; `.converting` + `replace` + `_ops/backup-hevc-originals`; original left in place on ffmpeg failure; pause is machine kv; encode linked only into CLI; no GPU in default tests.
- 4.3 second AC: `live-box` feature + `MEDIAOPS_LIVE`; runbook with destructive list; default CI offline.

**Not met as live proof, tracked as open item 3:** 4.3 first AC and `increments.md` “must run on this box”. Handoff D8 authorized marking 4.3 done on runbook+gate. This verdict does **not** treat a live demo as having run.

**Fix-now items 1–2** are code defects on the `run`→encode integration, not unfinished stories. They do not overturn 4.1/4.2 type-and-test ACs; they keep the epic at accepted-with-open-items rather than accepted.

A human may override to **rejected** if Story 4.3’s live Given/When/Then is blocking regardless of D8 (see Open questions).

## Open questions

- Should Story 4.3’s live SeedIt4Me/GPU AC block epic acceptance even though handoff D8 and the runbook say pending operator confirm? A yes flips this document’s verdict to **rejected**.
- Should `Action::Encode` in the plan become real later, or is after-install the permanent v1 shape (D7)?
- When (if ever) is NVENC session cap more than ffmpeg-encoders 0/1?

Analyses not resolved: no session logs, so “why a session swallowed `after_install`” is unread; live throughput unknown.

## Assumptions

Headless; no user confirmations.

- Epic selected from invocation: **4**. `detect-epic --epic 4` result recorded above; `pending_stories: []`.
- `project-context.md` glob matched nothing; continued.
- Phase 3 team discussion skipped.
- Parent-orchestration: did **not** run `sprint_status.py update`; did **not** edit `sprint-status.yaml` or any file other than this document.
- `git_evidence.py` story arrays empty → mapped commits from handoff subjects.
- Aggregate views and `bmad-review` lenses computed inline (no nested subagents); architecture graph from Cargo.toml + grep, not pydeps.
- Machine verdict **accepted-with-open-items** with no human decision: D8 amends 4.3 story-done to runbook+gate; live AC is an open item, not a silently passed demo; findings 8–9 are tracked fix-now items.
- Proposed action items 1–3 (owner Coto). No previous-retro `--set-action-status` transitions to propose.
- Behavior check: workspace offline tests + CLI help + live-box test list; no live box, no GPU.
