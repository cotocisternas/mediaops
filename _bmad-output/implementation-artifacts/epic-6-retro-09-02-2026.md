---
epic: 6
date: 09-02-2026
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 6 retrospective: Holds are an inbox

## Epic summary

Epic **6** (“Holds are an inbox”) — FR17 / CAP-8. Import-blocked releases are a typed operator inbox (age, size, *arr reason); Approve records a decision and exclusive `run` promotes through PathSchema; Reject is never-this-release (`DELETE queue/{id}?blocklist=true`). Auto-approve and agent-approve are forbidden in v1.

**Stories (both `done`; `pending_stories: []`):**

| Key | Title | Status | Spec |
| --- | --- | --- | --- |
| `6-1-hold-store-and-inbox-join` | Hold store and inbox join | done | `_bmad-output/implementation-artifacts/spec-6-1-hold-store-and-inbox-join.md` |
| `6-2-approve-and-reject` | Approve and Reject | done | `_bmad-output/implementation-artifacts/spec-6-2-approve-and-reject.md` |

**Diff range:** `3d11022^..8d1d79b` (includes the first story commit). `git_evidence.py --stories 6-1,6-2` returned `commit_count: 3`, `merge_count: 0`, `merges_measured: 0`, `binary_revisions: 0` on every path, `merge_files: []`.

**Commits (HEAD-first as the script emitted them):**

| SHA | Subject | `stories` from git_evidence | Manual attribution |
| --- | --- | --- | --- |
| `8d1d79b` | Close the Epic 6 sprint rows after PRs #7 and #8. | `[]` | sprint-status / README only |
| `df772be` | feat: add lock-free hold approve and reject (#8) | `[]` | story 6-2 (PR #8) |
| `3d11022` | feat: persist holds inbox and ship lock-free hold list (#7) | `[]` | story 6-1 (PR #7) |

`git_evidence.py` matches `--stories` with a word-boundary regex on the **subject**. None of the three subjects contain `6-1` or `6-2`, so the pre-pass could not attribute. Per-story file split below is from `git show --stat`, not from the JSON `stories` arrays.

- **6-1 (`3d11022`):** `holds_decisions` v6, `HoldKey` / `ReleaseId`, proto `HoldList`, `arr` importBlocked map, `sync::inbox`, lock-free `hold list --json`. 27 files, +2173 / −31.
- **6-2 (`df772be`):** lock-free `hold approve`/`reject`, `HoldReject`, placement/RemoteRef, PathSchema preflight, `plan_actions` Approved → Copy. 24 files, +2157 / −77.
- **Sprint close (`8d1d79b`):** README + sprint rows + context pointers. 5 files, +16 / −13. No product code.

**Highest `files` net (non-merge; this is change volume, not absolute size):** `crates/arr/src/apply.rs` +839, `bins/mediaops/src/hold.rs` +642, `crates/core/src/hold.rs` +325, `crates/proto/src/lib.rs` +323, `crates/net/src/seedbox.rs` +290, `crates/sync/src/plan.rs` +223, `bins/mediaops/tests/cli.rs` +220, `bins/mediaops/src/run.rs` +154. Total range: 34 files, +4275 / −50.

**Evidence inventory**

| Artifact | Status |
| --- | --- |
| Epic spec | Present: `_bmad-output/planning-artifacts/epics.md` `## Epic 6` + FR17. Criteria **declared**. |
| Story specs | Present, `status: done`. 6-1 baseline `793a74b`; 6-2 baseline `3d11022`. |
| Epic context | Present: `_bmad-output/implementation-artifacts/epic-6-context.md`. |
| Architecture / PRD | Spine AD-4 / AD-8 / AD-10 / AD-17; FR17 in `epics.md`. |
| Diff + git_evidence | Range established and measured. Story ids not in subjects (see above). |
| Sprint status | `epic-6: done`; both stories `done`; `epic-6-retrospective: optional`. No `action_items` key. |
| Previous retro | **Absent.** No `epic-*-retro-*.md`. Batch start. |
| Session logs | **Absent** for 6-1 / 6-2. Process-lesson analysis from session records is skipped. |
| `project-context.md` | **Absent** (`workflow.persistent_facts` glob matched nothing). |
| `bmad-review` | Ran **inline** (no nested subagents). Lenses: adversarial, edge-case-hunter, verification-gap. Structure/prose skipped (`applies_to: docs`). |

## Findings

Each item has a source. Dispositions: **fix now** / **defer** / **accept as-is**.

### Aggregate views

**Architecture delta** (inline: `git diff -- '*.rs'` import lines + `**/Cargo.toml` + `mediaops-arch-tests`; no pydeps / dependency-cruiser).

No `Cargo.toml` changed in the range, so no new workspace edges. `cargo test -p mediaops-arch-tests` passed 14 tests, including `live_workspace_is_ad2_subgraph` and `mediaopsd_to_store_is_violation`. New modules stay inside allowed crates: `core::hold`, `store::holds`, `sync::hold`, `bins/mediaops/src/hold.rs`. Additive `mediaops.v1` RPCs `HoldList` / `HoldReject` (`proto/mediaops.proto:62-63`, `181-207`). Join lives in `sync::inbox` (`crates/sync/src/hold.rs:8-14`); Servarr JSON mapping stays in `arr` (`crates/arr/src/apply.rs:909-924`); CLI calls `sync::inbox` (`bins/mediaops/src/hold.rs:17,66`) on an already-legal `mediaops → mediaops-sync` edge (`crates/arch-tests/src/lib.rs:31`). `ControlPort` / `GrabOps` each gained `hold_list` + `hold_reject` (`crates/core/src/control_port.rs:35-36,51-52`); every impl in the range was updated. No layering violation found.

No architecture-delta **defect**. The fat-trait growth is the duplication finding below.

**Duplication map**

1. **Near-identical `GrabOps` / `ControlPort` test doubles** in `bins/mediaops/src/hold.rs:208-256`, `bins/mediaops/src/run.rs:609-678`, `crates/sync/src/hold.rs:71-141`, `crates/sync/src/apply.rs:986+`, `crates/net/src/seedbox.rs:613-619`. Each new Control method will require another copy. **Defer.** Process lesson: one shared fake in `test_support`.
2. **Media-extension allowlists copied.** `is_media_extension` (`crates/arr/src/apply.rs:1073-1078`) and `is_hold_media_name` (`crates/net/src/seedbox.rs:377-387`) both special-case `mkv|mp4|m4v|avi|ts|flac|mp3|m4a`; seedbox also drops `sample`. **Accept as-is** for v1 (seedbox has the sample rule `arr` does not need).

**God-class / size growth** (ranked on `git_evidence.py` `files` net, then opened current size).

3. **`crates/arr/src/apply.rs` grew 1320 → 2159 lines** (`net +839`, two commits). The same module now owns grab set-diff apply, edge host rewrite, `LocalhostGrabOps::hold_list` / `hold_reject`, queue→`HoldLiveItem` mapping, placement/`schema_token`, and cassette tests. High churn makes it a candidate, not a verdict; the file is now a mixed grab+hold owner. **Defer** a split (`arr/src/hold.rs`) — not blocking FR17.

Other high-net files are new focused modules (`bins/mediaops/src/hold.rs` 642, mostly tests; `crates/core/src/hold.rs` 325) or additive conversions (`crates/proto/src/lib.rs` 823 → 1146). Not god-classes.

**Pattern divergence**

4. **Planner loads Approved holds with N `get_hold` calls** (`bins/mediaops/src/run.rs:279-286`) instead of `list_decided` + set membership, which `hold list` already uses (`bins/mediaops/src/hold.rs:58-66`). Same store, two query shapes. **Accept as-is** (live ∩ Approved is a different join than live ⊖ decided; correctness holds).

**Spec-to-implementation reconciliation** (declared AC in `epics.md` Epic 6 stories + FR17; story files).

| Declared | As-built | Call |
| --- | --- | --- |
| `holds_decisions` PK `(title_id, release_id)`, v5→6, refuse `>6` | `crates/store/src/lib.rs:20,349-360,300-306`; tests `v5_migrates_holds_decisions_keyed_by_title_and_release`, `future_schema_user_version_is_an_error` | met |
| `release_id` in `core`, wire verbatim, Servarr map only in `arr` | `crates/core/src/hold.rs:26-54`; `crates/proto/src/lib.rs:260-276`; `crates/arr/src/apply.rs:1140-1151` | met |
| Inbox = live ⊖ decided in `sync` only | `crates/sync/src/hold.rs:8-14`; CLI/plan consume it | met |
| `hold list --json` age/size/reason; lock-free; grabber=None empty | `bins/mediaops/src/hold.rs:18-25,66-75,277-294,353-382`; CLI `bins/mediaops/tests/cli.rs:723-774` | met |
| Approve persists decision, does not install; exclusive `run` Copy/install on PathSchema | `decide` at `hold.rs:93-152`; `plan.rs:103-153`; `run.rs:279-296`; `cmd_plan_approved_hold_copies_schema_path_not_scene_name` | met at plan; apply/install composition is finding 12 |
| Reject: `put(Rejected)` then DELETE `blocklist=true`; grabber=None usage | `hold.rs:135-137`; cassette `fixtures/arr/hold_reject_delete.json:4`; `seedbox.rs:317-320` | met (order is also finding 5) |
| PathSchema refuses spaces / leftover REPACJ, REPACK, PROPER | `crates/core/src/hold.rs:120-133,292-316`; CLI tests `hold.rs:469-528` | met when placement is present (finding 8) |
| No auto-approve / agent-approve / Research | CLI omits research (`tests/cli.rs:716-718`); string-search test `hold.rs:619-641`; `put_hold(..., Approved)` only in `decide` and tests | met; verification is weak (finding 13) |
| Blocked NZBs are not library; `_incoming` is not a hold folder | CLI lock-free tests assert schema dirs empty (`cli.rs:764-770,819-825`) | met |
| Epic context: inbox surface includes **ffprobe** (`epic-6-context.md:16`) | Story 6-1 JSON contract is `title_id, release_id, age_secs, size, reason` (`spec-6-1` Design Notes). No ffprobe on the hold path (encode crate only). | **Accept as-is** — story AC wins over context color. Record so later retros stop re-flagging. |
| Research stub or omit | Omitted | allowed |

### Diff-scope review (`bmad-review` inline)

**Narrowing:** nested subagents forbidden by parent orchestration. Lenses ran sequentially in this process over `3d11022^..8d1d79b` (34 files). Editorial lenses not applicable. Adversarial lens requires ≥10 concrete issues; empty would have been a re-check signal. Edge-case lens reports unhandled paths only. Verification-gap lens traces changed behavior to tests actually read.

#### Adversarial

5. **`location`:** `bins/mediaops/src/hold.rs:135-137` then `154-175`; frozen spec order in `spec-6-2` Always + `deferred-work.md:70-72`.
   **`trigger_condition`:** `put(Rejected)` (and Hold-job create/advance) commits before `Control.hold_reject`; RPC failure leaves the key out of the inbox with no retry.
   **`guard_snippet`:** persist after a successful reject, or record a retryable `reject_pending` and keep the key listed.
   **`potential_consequence`:** *arr still has the queue row; operator cannot `hold reject` again (unknown key).
   **Disposition:** **defer** (already ledgered; spec froze put-then-RPC).

6. **`location`:** `crates/arr/src/apply.rs:158-193`; `deferred-work.md:58-60`.
   **`trigger_condition`:** first sonarr/radarr/lidarr queue HTTP error fails the whole `HoldList` instead of returning the other apps’ rows.
   **`guard_snippet`:** per-app error → skip that app + trace; or declare fail-closed in the story.
   **`potential_consequence`:** one dead Lidarr hides Sonarr/Radarr import-blocked items.
   **Disposition:** **defer**.

7. **`location`:** `crates/store/src/holds.rs:24-41`; `deferred-work.md:66-68`.
   **`trigger_condition`:** one unparsable `title_id` / `release_id` in `holds_decisions` returns `StoreError` and fails `hold list`.
   **`guard_snippet`:** skip the bad row and trace, or quarantine it.
   **`potential_consequence`:** a corrupt row makes the entire inbox unreadable.
   **Disposition:** **defer**. Same shape on the wire: `TryFrom<HoldListResponse>` maps every item and fails the list (`crates/proto/src/lib.rs:404-409`).

8. **`location`:** `bins/mediaops/src/hold.rs:130-134` (`if let Some(placement)`); `crates/sync/src/plan.rs:103-110`; `deferred-work.md:74-76`.
   **`trigger_condition`:** Approve with no placement skips PathSchema preflight and still `put(Approved)`; `plan_actions` then `continue`s when remote or placement is missing (no Copy, no Skip).
   **`guard_snippet`:** usage/policy when Approved promotion data is missing; or keep the key listed until a RemoteRef exists.
   **`potential_consequence`:** operator “approves” a hold that never promotes and drops out of the inbox.
   **Disposition:** **defer**.

9. **`location`:** `crates/store/src/lib.rs:283-288`; `deferred-work.md:62-64`.
   **`trigger_condition`:** lock-free hold verbs share `state.db` with exclusive apply; no WAL; `busy_timeout` 5s.
   **`guard_snippet`:** `PRAGMA journal_mode=WAL` or a documented busy policy for lock-free readers.
   **`potential_consequence`:** `hold list` waits or errors while apply holds the write lock, looking like a hung inbox.
   **Disposition:** **defer**.

10. **`location`:** `crates/store/src/holds.rs:49-51`; `deferred-work.md:78-80`.
    **`trigger_condition`:** concurrent lock-free approve/reject on the same key last-write-wins via `ON CONFLICT DO UPDATE`.
    **`guard_snippet`:** insert-if-absent, or refuse a second decision.
    **`potential_consequence`:** racing approve+reject leaves a decision neither operator intended.
    **Disposition:** **defer**.

11. **`location`:** `bins/mediaops/src/hold.rs:163-166`; `crates/core/src/jobs.rs:140-171,533-536`; `crates/store/src/jobs.rs:161-165`.
    **`trigger_condition`:** each decide creates a `JobKind::Hold` keyed only by `title_id` (no `release_id`, `parent_job_id` must be None). Two releases of one title are two jobs with the same title and no join to `HoldKey`.
    **`guard_snippet`:** persist `release_id` on the job (or a hold-specific column) so `why` can name the release.
    **`potential_consequence`:** Epic 7 why-trace cannot tell which release a Hold job belongs to.
    **Disposition:** **defer** to Epic 7 (why-trace is FR6, not FR17).

12. **`location`:** `crates/arr/src/apply.rs:187` vs placement reader `1023-1043`; cassette `fixtures/arr/import_blocked_lidarr_queue.json:7,29`.
    **`trigger_condition`:** Lidarr queue fetch is `includeArtist=true&includeAlbum=true` (no `includeTrack`); placement requires nested `track`. The cassette inlines `track`, so the test does not prove live Lidarr will.
    **`guard_snippet`:** add the Lidarr include that actually nests tracks, or omit album holds without track rather than listing them with `placement: None` (which hits finding 8).
    **`potential_consequence`:** listed album holds approve without a PathSchema placement and never Copy.
    **Disposition:** **defer**.

#### Edge-case hunter (unhandled paths only)

Handled and discarded: grabber=None list empty; grabber=None reject usage; unknown key usage; leftover tags / spaces → exit 5 and no row when placement present; exclusive flock does not block list/approve/reject; `user_version > 6` refused; missing TitleId/`release_id` omitted; torrent vs usenet mapping; already-installed → `upgrade_never`; `file_len == 0` skipped; handshake on HoldList/HoldReject (`crates/proto/src/lib.rs:680-704`).

Unhandled (several overlap adversarial; listed once here if the path is the edge):

13. **`location`:** `crates/net/src/seedbox.rs:340-375`.
    **`trigger_condition`:** directory `outputPath` with zero or more than one non-sample media file leaves `remote` None (only `media.len() == 1` attaches).
    **`guard_snippet`:** surface a reason on the live item, or refuse approve until a unique file maps.
    **`potential_consequence`:** finding 8 — Approved with no RemoteRef, silent plan omit.
    **Disposition:** **defer**. The one-file case is tested (`seedbox.rs:714-743`).
    **`kind`:** not a deletion.

No deletion finding: the range is additive (`+4275 / −50`); removed lines are import churn and `PlanRequest` call-site updates. `plan_actions` gained `approved: &'a [HoldLiveItem]` with a single production caller (`bins/mediaops/src/run.rs:287-296`).

#### Verification-gap

14. **`gap_shape`:** regression-gap.
    **`location`:** exclusive `run` apply of an Approved hold — `crates/sync/src/plan.rs:103-153` + `crates/sync/src/apply.rs:94-108` + `bins/mediaops/src/run.rs:279-296`.
    **`trigger_condition`:** no test I read actually applies/installs an Approved hold; `cmd_plan_approved_hold_copies_schema_path_not_scene_name` (`bins/mediaops/src/run.rs:537-607`) asserts plan JSON Copy placement only.
    **`guard_snippet`:** an exclusive `run`/`apply` test that writes a scene-named remote file, `put_hold(Approved)`, and asserts the installed library path is PathSchema (no REPACK) and the file exists.
    **`potential_consequence`:** plan shape can be right while apply_copy/install never runs for this new candidate source; a regression that skips hold candidates in `apply` would not fail the tests I read.
    **`consumer`:** `apply` Copy arm at `crates/sync/src/apply.rs:94-108`, invoked from `run`.
    **`evidence`:** read `run.rs:537-607` (plan only); `plan.rs:709-766` (unit Copy, no FS); `hold.rs:396-467` (approve does not install, by design); searched `cmd_run` / `apply(` tests for Approved holds — none. Generic `apply_copy` listing tests exist in `crates/sync/src/apply.rs` but they do not construct `PlanRequest.approved`.
    **Disposition:** **fix now** (test-only remediation).

15. **`gap_shape`:** regression-gap.
    **`location`:** process-level `hold list --json` with a non-empty live set — `bins/mediaops/tests/cli.rs:723-774`.
    **`trigger_condition`:** the CLI binary test only exercises grabber=None empty inbox; the live⊖decided join at process level is untested.
    **`guard_snippet`:** a `cli.rs` test that starts loopback with `GrabOps` cassettes / FakeGrabOps and asserts `data.holds` is the undecided remainder.
    **`potential_consequence`:** a clap/dispatch regression that never calls `inbox` would still pass `hold_list_lock_free_json_empty_on_loopback`.
    **`consumer`:** `bins/mediaops/src/main.rs:808-818` → `hold::list`.
    **`evidence`:** `cli.rs:723-774` (empty); join is covered in-library by `hold.rs:296-351` (`decided_key_drops_out_of_inbox`) and `crates/sync/src/hold.rs:38-69`. Searched `cli.rs` for `hold list` — those two tests plus help.
    **Disposition:** **defer** (library-level join is tested; process-level is a thinner wrapper).

16. **`gap_shape`:** broken-verification-gap.
    **`location`:** `bins/mediaops/src/hold.rs:619-641`.
    **`trigger_condition`:** “no auto-approve” is `include_str!` substring search for `auto-approve` / `agent-approve` / `confidence_floor` in `apply.rs`, `plan.rs`, `run.rs`.
    **`guard_snippet`:** assert `put_hold` / `HoldDecision::Approved` is unreachable from `plan_actions` and the timer `run` path except via the CLI decide verb.
    **`potential_consequence`:** a timer that `put_hold(..., Approved)` without those substrings would not fail this test. Repo search today shows `HoldDecision::Approved` writes only in `decide` and tests (`hold.rs`, `run.rs:569` test helper) — so the named failure is currently absent, but the test would not catch it.
    **`consumer`:** unattended `run` (`bins/mediaops/src/run.rs`).
    **`evidence`:** read `hold.rs:619-641`; searched `put_hold` / `HoldDecision::Approved` in `*.rs`.
    **Disposition:** **accept as-is** for this epic (the named failure is not implemented; tightening is a later gate).

### Wins the evidence confirms

- Hold identity is `HoldKey {title_id, release_id}`, never a scene title (`crates/core/src/hold.rs:237-251`, `crates/arr/src/apply.rs:1880-1892`).
- Lock-free list/approve/reject succeed while an exclusive flock is held (`hold.rs:353-382,409-411,542-544`; `cli.rs:736-757,790-816`).
- Reject cassette pins `DELETE /sonarr/api/v3/queue/11?removeFromClient=true&blocklist=true` and refuses `downloadId` as path id (`hold_reject_delete.json:4`; `apply.rs:971-974,1983-1988,2126-2144`).
- `grabber=None` list is empty exit 0; reject is usage (`seedbox.rs:286-287,317-320`; `hold.rs:590-616`).
- Planner emits Copy, not `Action::Review`, and strips scene names (`plan.rs:736-762`).
- Research omitted from `hold --help` (`cli.rs:716-718`).
- AD-2 / no-store-in-mediaopsd still hold after the epic.

## Behavior verification

Runtime behavior **was** exercised offline. Live *arr approve/reject and live inbox join **were not**.

**Ran (all `--offline --locked`; no live-box feature, no seedbox SSH, no *arr HTTP):**

| Command | Result |
| --- | --- |
| `cargo test -p mediaops-store holds` | 2 passed (`v5_migrates_holds_decisions_keyed_by_title_and_release`, `put_get_and_list_decided_round_trip`) |
| `cargo test -p mediaops-store future_schema` | 1 passed |
| `cargo test -p mediaops-core hold` | 9 passed (identity, age, preflight, job hold path) |
| `cargo test -p mediaops-sync hold` | 7 passed (inbox matrix + approved Copy / upgrade_never / zero len) |
| `cargo test -p mediaops-arr hold` | 5 passed (cassette list/reject, mapping, empty keys) |
| `cargo test -p mediaops-net hold` | 3 passed (grabber=None vs Servarr, outputPath file and directory) |
| `cargo test -p mediaops hold` | unittests 11 passed; `tests/cli.rs` 4 passed (`hold_help`, empty lock-free list, unknown-key approve/reject usage); `tests/live.rs` 0 selected |
| `cargo test -p mediaops cmd_plan_approved_hold` | 1 passed (plan JSON only) |
| `cargo test -p mediaops-arch-tests` | 14 passed |

**Observed:** store migrates v5→6 and refuses future schema; inbox join drops decided keys; CLI `hold list --json` on loopback grabber=None returns `{ok, data.holds: []}` exit 0 with flock held and no schema writes; approve/reject unknown key exit 2; cassette reject hits `queue/{id}?blocklist=true`; Approved+remote plans a PathSchema Copy.

**Not exercised:** live seedbox Control `HoldList` joined against a real Servarr import-blocked queue; live `hold approve` / `hold reject` against *arr (explicitly out of scope). Composition `outputPath → RemoteRef → put(Approved) → exclusive run apply/install` is not one end-to-end test (finding 14).

## Previous-retro follow-through

Nothing to follow through on, and which kind:

- **No prior retro document.** No `_bmad-output/implementation-artifacts/epic-*-retro-*.md` (batch start; parent stated previous retrospectives do not exist yet).
- **`sprint-status.yaml` has no `action_items` array** (only a comment at line 30 that retros append there). Not “no outstanding items in an existing list” — the key is absent.

No `--set-action-status` transitions would have been proposed. Headless: the flag is not passed.

## Action items

Proposed, not applied. Sprint-status append skipped (parent-orchestration constraint: do not run `sprint_status.py update`, do not edit `sprint-status.yaml`).

| # | Action | Owner | Kind |
| --- | --- | --- | --- |
| 1 | Add an exclusive `run`/`apply` test that installs an Approved hold onto a PathSchema path (scene-named remote file → library path without leftover scene tags). Closes finding 14. | Coto | remediation (test) |
| 2 | Put story keys (`6-1`, `6-2`, …) in commit subjects so `git_evidence.py --stories` can attribute. This range’s three subjects left `stories: []`. | Coto | process |

Deferred findings 5–13 and 15 remain tracked in `_bmad-output/implementation-artifacts/deferred-work.md` (Epic 6 split, 2026-09-02) plus this document. They are not duplicated as new sprint action items.

## Acceptance verdict

**accepted-with-open-items** (machine; no human override). **Criteria: declared** (not profiled).

Declared bar: FR17 (`epics.md:61`) and Epic 6 story AC (`epics.md:656-694`; story files Tasks & Acceptance).

**Why not rejected:** `pending_stories` is empty. Offline tests demonstrate list/join, lock-free JSON, PathSchema refuse when placement exists, reject cassette blocklist, grabber=None usage, no Research, no store-in-mediaopsd, and Approved → Copy (not Review). Blocked NZBs are not written under movies/series/music/`_incoming` in the tests that hold a flock and run hold verbs.

**Why not accepted:** named findings remain deferred and tracked (findings 5–13, 15, already in `deferred-work.md`) plus two proposed action items. Exclusive `run` apply/install of an Approved hold is specified (`spec-6-2` AC: “when exclusive `run` applies, then Copy/`install` lands on a PathSchema path”) and is proven at **plan** shape, not apply. Live inbox join was not exercised.

Unfinished stories: none.

## Open questions

- Should `HoldList` fail closed on one *arr HTTP error, or skip that app? Unspecified; finding 6.
- Should Hold jobs carry `release_id` for Epic 7 `why`, or is `holds_decisions` the only identity?
- Is ffprobe on the inbox a later story, or was epic-context color only?
- When will live inbox join (seedbox Control + real import-blocked queue) be exercised on this box?

## Assumptions

Headless run; no user confirmation.

- Epic **6** taken from the invocation (not auto-detect). Parent supplied `detect-epic` result: `{"epic": 6, "story_count": 2, "pending_stories": [], "retro_key": "epic-6-retrospective", "retro_status": "optional"}`. Completeness gate ran; pending list empty.
- Mode: sprint. Phase 3 (team discussion) skipped. `bmad-party-mode` not invoked.
- `project-context.md` absent; continued.
- Diff range assumed `3d11022^..8d1d79b` after confirming those three commits are the Epic 6 first-parent spine. Story attribution is manual because git_evidence subjects did not match `6-1`/`6-2`.
- `bmad-review` ran inline over this epic’s diff (parent: no nested subagents). Architecture delta without a language-native graph tool: Cargo.toml + arch-tests + import diff.
- Machine verdict **accepted-with-open-items** rendered with no human decision. Not silently **accepted**.
- Proposed action items (above) recorded here only. Did **not** run `sprint_status.py update`, did **not** pass `--set-action-status`, did **not** edit `sprint-status.yaml` (parent-orchestration).
- Did **not** approve/reject live *arr holds. Live inbox join not exercised.
- Session-log process analysis skipped (logs absent).
- No prior retro follow-through (no retro doc, no `action_items` key).
