---
epic: 5
date: 09-02-2026
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 5 retrospective — Quiet box without the panel

## Epic summary

Epic 5 (`Quiet box without the panel`) is sprint-mode, stories `5-1`…`5-4` all `done`, `pending_stories: []`. `detect-epic --epic 5` returned `retro_key: epic-5-retrospective`, `retro_status: optional`.

**Diff range (main, delivered):** `793a74b^..793a74b` (`e1d9b6b..793a74b`). GitHub squash-merge of PR #6 (`Epic 5: Quiet box without the panel`). One parent, not a merge commit. `git_evidence.py` on this range: `commit_count: 1`, `merge_count: 0`, `merges_measured: 0`, `stories: []` (squash subject does not name `5-1`…`5-4`).

**Same tree as the branch tip:** `793a74b^{tree} == a77e547^{tree}`. Original first-parent story commits (still in the object store / PR #6):

| Story | SHA | Subject |
| --- | --- | --- |
| 5.1 | `001d80f` | Add the arr crate over HttpTransport. |
| 5.2 | `bf37f05` | Apply grabber sets as a diff and discover keys at runtime. |
| 5.3 | `bcbfc79` | Enforce EdgeInvariant with read-only doctor and confirmed repair. |
| 5.4 | `ba27b66` | Add seedbox upgrade and a pin matrix that can refuse. (amended from `4ec20fb`) |
| review | `a77e547` | Close PR #6 review findings for Epic 5. |

`git_evidence.py --range 001d80f^..a77e547 --stories 5-1,5-2,5-3,5-4`: `commit_count: 5`, `merge_count: 0`, still `stories: []` (subjects do not contain those ids). Per-story mapping above is from PR #6 / reflog, not from the script’s subject parser.

Churn (non-merge, squash range): 48 files, **+8459 / −129**. Hottest paths: `crates/arr/src/apply.rs` (+1320), `crates/arr/src/servarr.rs` (+839), `Cargo.lock` (+736), `crates/core/src/desired_state.rs` (+492 net), `crates/arr/src/qbit.rs` (+384), `crates/arr/src/keys.rs` (+352), `bins/mediaops/src/doctor.rs` (+290), `bins/mediaops/src/bootstrap.rs` (+278 net), `crates/arr/src/sab.rs` (+283), `crates/arr/src/lib.rs` (+273).

**Stories completed:** `5-1-arr-crate-over-httptransport`, `5-2-grab-apply-as-set-diff`, `5-3-edge-apply-doctor-and-repair`, `5-4-seedbox-upgrade-and-version-pins`. Unfinished: none.

**Declared criteria** (not profiled): story ACs in `_bmad-output/planning-artifacts/epics.md` § Epic 5, plus FRs FR4, FR5, FR10 (packages/edge/grabber slice), FR11, FR22, FR23, FR26, NFR2, NFR5, NFR13, AD-15.

### Evidence inventory

| Item | Status |
| --- | --- |
| Epic spec | Present: `epics.md` § `## Epic 5: Quiet box without the panel` (stories 5.1–5.4 ACs). Context: SPEC.md quiet-box / EdgeInvariant; grabber-inventory.md; failure-history-tests.md; ARCHITECTURE-SPINE.md AD-15 / doctor-split / AD-22. |
| Story files | **Missing.** No `spec-5-*.md` under `{implementation_artifacts}`. Intent lives only in `epics.md` + PR #6 body. |
| Diff range | Established: `793a74b^..793a74b`. Original `001d80f^..a77e547` same tree. |
| Sprint status | `{implementation_artifacts}/sprint-status.yaml`: epic-5 `done`, four stories `done`, `epic-5-retrospective: optional`. No `action_items` key. |
| Previous retrospective | **None.** No `epic-*-retro-*.md`. Batch start. |
| Session logs | **Absent.** No epic-5 conversation/session artifacts. Process-lesson analysis from logs skipped. |
| Existing review | `{implementation_artifacts}/review-pr-6-epic-5.md` (chunks 5.1–5.4, all items `[x] [Review][Patch]`). Re-checked against `793a74b` / HEAD. |
| `project-context.md` | **Absent** (glob `{project-root}/**/project-context.md`). |
| Live box | **Not exercised.** PR #6: “No live SeedIt4Me host.” This run did not SSH, write nginx, or run doctor/repair/upgrade on a box. |

HEAD at document time is `8d1d79b` (Epic 6 already on main). Epic 6 grew `crates/arr/src/apply.rs` (+841) and `crates/net/src/seedbox.rs` (+306). Offline tests ran on HEAD; Epic 5 surfaces still pass. Line citations for grab/edge apply prefer `793a74b` where Epic 6 shifted numbers.

## Findings

### Aggregate views

#### Architecture delta

The epic adds the `mediaops-arr` crate and wires grabber HTTP only through `HttpTransport`. `mediaopsd` constructs `ReqwestTransport` + `LocalhostGrabOps` (`bins/mediaopsd/src/main.rs:261–278`). The CLI crate does **not** depend on `arr` (`bins/mediaops/Cargo.toml`). `arch-tests` `live_workspace_is_ad2_subgraph` passed: `reqwest` is a direct dep of `mediaops-arr` only.

Proto: `EdgeApply` is new (`proto/mediaops.proto` vs `e1d9b6b`). `GrabApply` / `EdgeCheck` / `KeyDiscovery` existed as stubs and gained real handlers. Seedbox Control now takes `grab_ops` and `nginx_dir` (`crates/net/src/seedbox.rs:31–54`). The systemd unit gained `--nginx-dir /etc/nginx/apps` (`bins/mediaops/src/bootstrap.rs:387–389`, asserted at `:883`). `core` gained `similar` unified diffs (`crates/core/src/diff.rs`). `ssh` gained Host `$host` splice + `copy_binary_and_restart_unit`. `sync` planner emits `GrabApply` / `EdgeApply` when grabber is Servarr / freeze (`crates/sync/src/plan.rs:185–188`).

No new crate-graph cycle. Layering matches AD-4: CLI → UDS → home gateway → seedbox Control → localhost *arr HTTP.

**Narrowing:** no language-native dep-graph tool (pydeps/madge) was run; the delta is from the changed Cargo.toml files plus `arch-tests`.

#### Duplication map

`url_base` / bind / auth defaults (`/sonarr`, `127.0.0.1`, `forms`) are restated in `LocalhostGrabOps::new`, `check_edge_apps`, `apply_edge_host`, and `bins/mediaops/src/repair.rs` app loop. Nginx “sudo cat live, then write” is duplicated between `write_remote_file` and `write_spliced_nginx_app` (`crates/ssh/src/lib.rs:205–266`). Fingerprint freeze is computed in `doctor::is_frozen` and called from doctor, `seedbox_apply`, and `run` — one helper, three call sites (not independent reimplementation).

Disposition: **accept as-is** (convention, not a defect).

#### God-class / size growth

`git_evidence.py` `files` is change volume on non-merge commits. Ranked, then opened at `793a74b`:

| Path | Epic-5 size | Before | Note |
| --- | --- | --- | --- |
| `crates/arr/src/apply.rs` | 1320 (new) | 0 | Grab set-diff + edge API + tests. HEAD 2159 after Epic 6 holds. |
| `crates/core/src/desired_state.rs` | 958 | 467 | grab/edge/paths/pins tables. |
| `bins/mediaops/src/bootstrap.rs` | 1106 | 828 | upgrade + unit `--nginx-dir`. |
| `crates/arr/src/servarr.rs` | 839 (new) | 0 | Shared Servarr client. |
| `crates/ssh/src/lib.rs` | 708 | 460 | nginx splice + musl copy. |
| `bins/mediaops/src/main.rs` | 883 | 683 | doctor / repair / apply / upgrade verbs. |

`apply.rs` is the epic’s growth hotspot — one module now owns set-diff, edge host PUT, and (later) hold mapping. Not a 3k-line class. Disposition: **accept as-is**; split is not required for this verdict.

#### Pattern divergence

Grab apply reports `+sonarr indexer NZBgeek` style lines (`793a74b:crates/arr/src/apply.rs` `apply_indexers`). Edge host PUT and nginx writes use `core::unified_diff` (`crates/arr/src/apply.rs` `apply_edge_host`; `crates/ssh/src/lib.rs:226`). FR5 names ini/xml/nginx, not JSON set-diff, so the split is consistent with the declared FR. Disposition: **accept as-is**.

`ReqwestTransport` lives in `crates/arr/src/reqwest_impl.rs` with `reqwest` as a **direct** `mediaops-arr` dependency. Story 5.1 AC and AD-15 say “the reqwest impl is linked only inside mediaopsd”. AD-2 / `arch-tests` / PR #6 body say reqwest belongs under `arr`. As-built follows AD-2. See F2.

#### Spec-to-implementation reconciliation

Story ACs 5.1–5.4 are largely implemented (HttpTransport + cassettes, set-diff + key discovery + CLI via UDS, doctor read-only + repair `--repair --confirm`, upgrade musl copy + pin refuse + proto handshake). Divergences:

- **F2 — AD-15 vs AD-2 reqwest placement.** Source: AD-15 (`ARCHITECTURE-SPINE.md:159`), story 5.1 AC (`epics.md:579`), as-built `crates/arr/Cargo.toml:9` and `arch-tests` `reqwest_outside_arr_is_violation`. Disposition: **accept as-is**; spec reconciliation proposed (action item 2).
- **F3 — FR4 Paths apply.** `DesiredState` parses `[[paths.roots]]` (`crates/core/src/desired_state.rs:353–389`) and `ArrClient::root_folders` exists, but nothing applies root folders as a set-diff. Epic intro and FR4 name “Edge/Grab/Paths apply”. No story AC required Paths apply. Disposition: **defer** (action item 3).
- **F4 — FR10 packages.** Epic intro: “Packages/nginx that bootstrap skipped beyond daemon install land here.” Nginx splice landed; no *arr/Swizzin package install (5.4 AC forbids panel/apt for **mediaopsd** upgrade). Disposition: **defer** (action item 3).
- **F12 — scheduled doctor.** Doctor is a lock-free read-only CLI (`bins/mediaops/src/doctor.rs:97–100`, `:133`). No `mediaops-doctor.timer`. FR22 says “scheduled”; the doctor-split convention is “when it runs, it is read-only,” not “ship a timer.” Disposition: **accept as-is**.
- **F13 — SPEC.md extra doctor invariants.** SPEC.md / grabber-inventory name qBit DHT/PeX/LSD off and SAB `tv`/`movies`/`music` as doctor invariants. Story 5.3 AC is bind + `url_base` + Host `$host` + Forms + Prowlarr URLs. Doctor/`edge_api_check` implement the story AC only (`crates/arr/src/apply.rs` `check_edge_apps` at `793a74b`). Disposition: **defer**.

### Diff-scope review (bmad-review lenses, inline)

`bmad-review` was not spawned as a nested subagent (parent constraint). Lenses **adversarial**, **edge-case-hunter**, and **verification-gap** ran inline over the Epic 5 diff (`793a74b^..793a74b` plus original story commits for boundaries). Editorial structure/prose lenses skipped (`applies_to: docs`). `project-context.md` missing noted in the header. Existing `review-pr-6-epic-5.md` items were re-opened against primary sources; patched items that still hold are not re-listed as open findings.

**Narrowing:** lenses ran sequentially in this process over the epic range, not one subagent per lens; no full `git diff` dump was re-reviewed hunk-by-hunk beyond the files named below.

#### Adversarial

1. **Bootstrap install edge check is fail-open.** `bins/mediaops/src/bootstrap.rs:249–259` (`793a74b` same): `if let Ok(channel) = connect_home(...)` then `if let Ok(edge) = control.edge_check()` then refuse only if `!invariant_ok`. Connect or RPC failure → bootstrap still `applied: true`. Story 5.3 AC: “after install/upgrade an edge check is queued before success.” Upgrade path is fail-closed with retries (`:301–334`). Trigger: home socket down or `nginx_dir` missing during first bootstrap. Guard: match upgrade (retry, then `BootstrapError`). Consequence: install reports success with EdgeInvariant unverified.
2. **Custom-format create is `{"name"}` only; scores need `quality_profile`.** `793a74b:crates/arr/src/apply.rs:378` `post_custom_format(&json!({"name": name}))`; scores applied only inside `if let Some(profile_name) = &desired.grab().policy.quality_profile` (`:387`). Live Servarr typically requires specifications; packs without a profile name never PUT `formatItems`.
3. **Doctor drops `--desired-state`.** `bins/mediaops/src/doctor.rs:111` `let _ = desired_state;`. `edge_api_check` uses `LocalhostGrabOps` built at daemon start (`bins/mediaopsd/src/main.rs:277`), not the CLI file. Operator edits to bind/`url_base` are invisible to doctor until mediaopsd restarts.
4. **Empty indexer/client want is a no-op, not “delete extras.”** `793a74b:crates/arr/src/apply.rs:236` and `:295` `if want.is_empty() { return Ok(Vec::new()); }`. Per-app skip avoids wiping Radarr when only Sonarr indexers are listed; an explicit empty set for one app cannot clear live rows. Story 5.2 AC “delete extras” is therefore scoped to non-empty want.
5. **qBit non-login HTTP 4xx echoes the body.** `crates/arr/src/qbit.rs:107–111` (`String::from_utf8_lossy(&resp.body)`). Login errors are generic (`:48–50`). Cookie/SID can appear in later error Display.
6. **Upgrade pin matrix does not consult live OS/glibc.** `bins/mediaops/src/bootstrap.rs:282` `pin_matrix_refuse(ds.pins())` → `pin_matrix_refuse_live(..., None, None)` (`crates/core/src/desired_state.rs:220–222`). `live_glibc < glibc_min` is dead on the upgrade verb. Named Lidarr trap is TOML `pins.lidarr` vs `refuse_above` only.
7. **`paths.roots` are parsed and never applied** (same as F3).
8. **CLI second-apply test never injects Servarr `GrabOps`.** `bins/mediaops/src/apply_cmd.rs:97–127` uses `DS_UNLOCKED` + `start_pair` (`Grabber::None`). Asserts `noop: true` twice via handshake, not set-diff. Real set-diff noop is `arr::apply::tests::second_apply_is_noop_when_sets_match`.
9. **Repair is nginx writes then API; no rollback.** `bins/mediaops/src/repair.rs:65–107`. AC calls it “one transaction.” A failed `edge_apply` after spliced nginx leaves Host `$host` written.
10. **`HOME` unset refuses Servarr grab ops.** `bins/mediaopsd/src/main.rs:272–274`. Fail-closed. Daemon under systemd user normally has `HOME`; a stripped env yields a runtime error instead of key absence.
11. **url_base defaults repeated** (duplication map) — operator-visible if one site is updated and another is not.

#### Edge-case hunter

Unhandled (or only partially handled) paths from the diff:

- **Empty want per app** — skip, not delete (`793a74b:crates/arr/src/apply.rs:236`, `:295`).
- **`quality_profile` absent** — `pack.scores` never written (`:387`).
- **`connect_home` / `edge_check` `Err` during bootstrap** — success (`bootstrap.rs:249–259`).
- **Doctor `desired_state` present** — discarded (`doctor.rs:111`).
- **`pin_matrix_refuse_live` with live glibc** — not called from `upgrade()`.
- **qBit 4xx body** — unredacted (`qbit.rs:107–111`).
- **Indexer want empty on one app, non-empty on another** — unmanaged app’s live indexers kept (handled by the skip; documented as F8).

Deletion check: the epic is additive (new crate + verbs). No retired contract found in the squash vs `e1d9b6b` besides stub `GrabApply`/`EdgeCheck` becoming real.

Patched since `review-pr-6-epic-5.md` and **not** re-opened: SAB key in cassette path; `ReqwestTransport` timeout/redirect/8 MiB cap (`reqwest_impl.rs:17–21`, `:54–81`); qBit `Fails.` login; masked keys in `fields[].value`; 401/409 through `ArrClient`; Debug redaction (`transport.rs:15–50`); PEM `.crt`/`.key` + `read_dir` errors; Host `$host` splice; `--nginx-dir` on the unit; `skip_edge: false` upgrade test; musl cargo argv assertion.

#### Verification-gap

- **Broken-verification — CLI second apply.** `apply_cmd::tests::seedbox_apply_second_call_is_noop` would still pass if Servarr set-diff always mutated. Consumer: `mediaops seedbox apply --json`. Evidence: test body uses `DS_UNLOCKED` / `start_pair`; contrasting observing test is `crates/arr/src/apply.rs` `second_apply_is_noop_when_sets_match` (cassette POSTs then matching GETs). `gap_shape: broken-verification-gap`.
- **Regression-gap — CF scores.** `cf_pack_policy_and_edge_apply_mutate_then_noop` POSTs a CF and PUTs delay/host; it does not set `grab.policy.quality_profile` and does not assert `formatItems`. Dropping `apply_quality_profile_scores` would not fail that test. `gap_shape: regression-gap`.
- **Regression-gap — bootstrap install edge.** No test fails when post-install `edge_check` errors; the `if let Ok` swallows it. Upgrade has `upgrade_runs_edge_check_and_persists_fingerprint`. `gap_shape: regression-gap`.
- **`every_named_arr_resource_has_a_cassette_hit`** (`crates/arr/src/lib.rs:86–213`) pushes in-memory 200s; it does not prove workspace `fixtures/arr/*.json` cover grabber-inventory. Named HTTP failures *do* have workspace files (`masked_key_test.json`, `duplicate_nzbgeek.json`, `indexer_test_unauthorized.json`) and `workspace_cassettes_replay_named_http_failures`. Not filed as a gap beyond the note.
- **Live doctor/repair/upgrade/nginx** — no test in this run hit a real box (AD-20). Recorded under Behavior verification, not as a missing unit test.

### Existing review re-check (`review-pr-6-epic-5.md`)

All 5.1–5.4 bullets are `[x] [Review][Patch]`. Re-opened against `793a74b` / current sources:

- **Still patched:** key redaction, cassette miss, reqwest policy, qBit login/`Fails.`, masked field keys, 409 mapping, wanted/cutoff, query encode, `http_error` UTF-8 truncate, qBit omitted privacy flags, qBit `metadata` not `is_file`, unclosed `ApiKey`, empty keys, SAB `status: false`, nameless indexers, Prowlarr `application_url_ok` path-segment, Sonarr seasons via `series/{id}`, Debug redaction, cassette `read_dir`, host config required fields, paging, GET-merge-PUT indexers/clients, nginx splice + `nginx -t`, freeze at `seedbox_apply`, Prowlarr URL vs Sonarr base, `unified_diff` before host PUT, PEM scan errors, `::` bind, pin `parse_semver` refuse on junk patch, `accept_handshake` skew warn, `skip_edge: false` test, musl argv, upgrade retry + fingerprint write, unit `--nginx-dir`.
- **Residual (routed below):** CLI second-apply still handshake-noop; CF create still `{name}` (scores now go to a named quality profile only); bootstrap install edge check added but fail-open.

## Behavior verification

Offline only. No live SeedIt4Me, no nginx write, no `doctor`/`repair edge`/`seedbox upgrade` against a running box. AD-20.

Ran on HEAD `8d1d79b` (Epic 6 present; Epic 5 tests still in tree):

| Command | Result |
| --- | --- |
| `cargo test --locked --offline -p mediaops-arr -p mediaops-arch-tests` | 67 arr + 14 arch-tests passed (incl. AD-2 live workspace, named cassettes, set-diff, bind-to-star, duplicate NZBgeek, masked keys, qBit privacy, SAB status:false) |
| `cargo test --locked --offline -p mediaops --bins` | 62 passed: doctor read-only + unattended `--repair` + PEM + freeze; repair confirm + fingerprint persist (ssh **transcript**); `seedbox_apply` freeze + second-call noop (handshake); Lidarr glibc refuse; upgrade `--yes` copy; `upgrade_runs_edge_check_and_persists_fingerprint` (`skip_edge: false`, loopback) |
| `cargo test --locked --offline -p mediaops-core --lib -- lidarr_glibc pin_matrix desired_state nginx_host unified_diff` | 18 passed |
| `cargo test --locked --offline -p mediaops-ssh --lib -- splice nginx Host upgrade musl` | 4 passed (Host `$host`, splice keeps conf, upgrade never apt/panel, musl target on cargo argv) |
| `cargo test --locked --offline -p mediaops-proto -- handshake minor_skew` | `handshake_rejects_unknown_package` passed |
| `cargo test --locked --offline -p mediaops --test cli` | 23 passed; `doctor_repair_unattended_from_a_public_laptop` passed |
| `cargo test --locked --offline -p mediaops-net --lib -- seedbox` | 9 passed incl. `panel_host_rewrite_changes_fingerprint_and_drifts`, grabber=None GrabApply handshake |
| `cargo test --locked --offline -p mediaops-sync --lib -- GrabApply EdgeApply edge_frozen` | `edge_frozen_emits_edge_apply_first` passed |

**Explicitly not run:** live `mediaops doctor`, `repair edge`, `seedbox upgrade`, nginx `-t`/reload, or any SSH to a real host. TranscriptExec fakes ssh/scp/cargo.

Observed: named failure-history rows that this epic owns (masked keys, duplicate NZBgeek, bind-to-star / Prowlarr `/{id}/`, panel Host rewrite fingerprint, Lidarr glibc trap, `doctor --repair` unattended, cert PEMs in git, Autobrr/Bazarr absent) have offline tests that passed. Second apply as a **Servarr set-diff** is proven in `mediaops-arr`, not at the CLI loopback.

## Previous-retro follow-through

Nothing to follow through:

- No prior retrospective document (`{implementation_artifacts}/**/epic-*-retro-*.md` empty at batch start).
- `sprint-status.yaml` has **no** `action_items` key (only a comment that retros append there).

No `--set-action-status` transitions would have been proposed.

## Action items

Proposed, not applied (headless). Remediation and spec reconciliation await a human.

1. **Fail-closed edge check after `seedbox bootstrap` install** — Owner: Coto. Match `upgrade()`: retry Control, then error if `edge_check` cannot run or `!invariant_ok`; persist `EDGE_FINGERPRINT_KEY` on success. Evidence: `bins/mediaops/src/bootstrap.rs:249–259` vs `:301–343`; story 5.3 AC. *Remediation.*
2. **Reconcile AD-15 / story 5.1 AC with AD-2** — Owner: Coto. As-built and `arch-tests` put `reqwest` on `mediaops-arr`; mediaopsd is the only production constructor of `ReqwestTransport`. Rewrite AD-15 / 5.1 AC to that, or move the impl crate-boundary (not recommended). Evidence: `crates/arr/Cargo.toml:9`, `ARCHITECTURE-SPINE.md:159`, `epics.md:579`. *Spec reconciliation.*
3. **Reconcile FR4 Paths apply and FR10 packages** — Owner: Coto. Either add a later story (root-folder set-diff; Swizzin package install) or record in the spec that Epic 5 delivered nginx splice + grab/edge API only, with *arr packages remaining provider-already-there. Evidence: `epics.md:154`, `:160`, `:196`, `:565–567`; no apply of `paths.roots`. *Spec reconciliation.*
4. **Observe Servarr set-diff on the CLI apply path** — Owner: Coto. `start_pair_with_grab_ops` already exists (`bins/mediaops/src/test_support.rs:77–81`); use it in `seedbox_apply_second_call_is_noop` so a broken GrabOps still fails the CLI test. Evidence: `bins/mediaops/src/apply_cmd.rs:97–127`. *Remediation / verification.*

Deferred (not action items): CF `{name}` POST and scores-only-with-quality-profile; doctor ignoring desired-state; qBit 4xx bodies; pin matrix live glibc; SPEC.md qBit/SAB doctor extras; `apply.rs` split.

## Acceptance verdict

**accepted-with-open-items** — criteria **declared**.

Machine verdict (no human override in this headless run):

- Completeness: `pending_stories` empty; all four sprint keys `done`. Does not force `rejected`.
- Story 5.1: HttpTransport-only arr crate, cassettes for masked keys / duplicate NZBgeek, Autobrr/Bazarr absent, reqwest rustls. **Met** (reqwest lives in `arr`, see F2).
- Story 5.2: set-diff + duplicate conflict + runtime key discovery + masked refuse + `seedbox apply` over home UDS + second apply no-op when sets match (arr crate). **Met** with F7/F8 notes.
- Story 5.3: unified diffs before nginx/host write; EdgeInvariant bind/`url_base`/Host `$host`/Forms/Prowlarr URLs; doctor read-only + PEM git refuse; fingerprint freeze; `repair edge --repair --confirm`; `doctor --repair` unattended named failure. **Mostly met**; install-time edge check is fail-open (F1).
- Story 5.4: upgrade recopies musl binary + restarts unit, never apt/panel; proto package refuse; minor skew warn; Lidarr pin refuse exit 5. **Met** offline.
- FR5, FR11, FR22 (read-only doctor), FR23, FR26, NFR2, NFR5, NFR13: met in the offline evidence, with F12’s timer reading.
- FR4 Paths + FR10 packages: **not met as epic-prose**; not in story ACs. Tracked as open items, not a reject.

Blocking findings: none that fail the four story contracts once F1 is tracked as open. Live box unexercised is an AD-20 scope limit, not a silent accept of production nginx.

## Open questions

- Should FR4 Paths apply and FR10 grabber-stack packages be later stories or dropped from the Epic 5 FR list?
- Should doctor grow qBit DHT/PeX/LSD and SAB category checks (SPEC.md) or stay on the story 5.3 EdgeInvariant subset?
- Is bootstrap’s fail-open edge check intentional (home gateway may not be up yet) or a bug relative to “queued before success”?
- When should CF pack `scores` apply if `quality_profile` is omitted?

## Assumptions

Headless run; no user confirmation.

- Epic **5** taken from the invocation (not auto-detect). `detect-epic --epic 5` → `story_count: 4`, `pending_stories: []`, `retro_key: epic-5-retrospective`, `retro_status: optional`. `done_stories` in that JSON also lists other epics’ done keys; completeness used only this epic’s four story keys (all `done`).
- Sprint mode (`--epic 5`), not stories mode.
- Diff range `793a74b^..793a74b`; original story SHAs used for attribution because the squash subject has no `5-1`…`5-4`.
- Machine verdict **accepted-with-open-items** with **no human decision**. Unfinished-story fail-safe did not fire.
- Phase 3 team discussion skipped.
- `bmad-review` lenses run inline; no nested subagents.
- `project-context.md` absent; continued.
- Session logs absent; process lessons from logs skipped.
- Previous retro / `action_items` absent; nothing to follow through; `--set-action-status` not used.
- `sprint_status.py update` **not** run (parent orchestration). Retro key remains `optional` in yaml.
- Proposed action items 1–4 recorded here only.
- Live doctor/repair/upgrade/nginx **not** exercised; assumed AD-20 offline suite is the bar this epic set for itself (PR #6 same constraint).
- Tests executed on HEAD (Epic 6 already merged); Epic 5 assertions still pass.
- Date `09-02-2026` from the invocation.
