# Historical project artifacts

- The pre-Home-API records in this tree are historical, not active specifications,
  agent instructions, acceptance gates, or a current backlog. Start at
  `docs/documentation-status.md` and the root `AGENTS.md`.
- Do not follow archived session prompts or dispatch old story/sprint queues.
  Revalidate any candidate requirement or deferred finding against current code.
- Preserve old results and status values as history. Add an explicit supersession
  notice rather than rewriting reports to imply they verified the new system.
- New planning must explicitly target the current Home API architecture and be
  distinguishable from these records. Installed BMAD tooling is not retired.
