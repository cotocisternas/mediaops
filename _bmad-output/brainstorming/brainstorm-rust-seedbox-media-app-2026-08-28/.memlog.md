---
topic: Rust app to replace seedbox and media-sync scripts
goal: One contained system: remote seedbox desired-state (SSH, maybe forwards), install/remove/configure *arr+usenet+torrent, bootstrap local library, and CLI LLM agents for research/debug
mode: autonomous
status: complete
updated: 2026-08-28T22:46
---

- (direction by user) Build a Rust app to replace the custom seedbox and media-sync scripts in a formal, contained system
- (direction by user) Modules in mind: remote seedbox config (port forwarding?), install/remove *arr/usenet/torrent, configure apps and directories, bootstrap a local media path, invoke CLI LLM agents for title research and media debug
- (note) Session grounded in live tree under ~/videos, apply-stack.py, media-sync, encode_h264.py, reclaim, SEEDBOX.md, and prior seedbox research
- (technique) started First Principles Thinking
- (insight) Bedrock: two machines, one operator; seedbox is rented working memory; home disk is the archive and the playback surface
- (insight) Bedrock: *arr is a grabber, not the library of record; local filesystem plus Jellyfin is the library of record
- (insight) Bedrock: installers mutate files and are not desired state; panel rewrites nginx; Usenet is deletable after copy; torrents are not while seeding
- (insight) Bedrock: SSH is the trust boundary and the pipe; nginx URL-bases already are the UI edge; naming is a Jellyfin contract; Chrome/TV need H.264 8-bit
- (idea by coach) Model the world as two Stores (RemoteBuffer, LocalArchive) plus an optional Grabber; the app is a reconciler, not a script runner
- (idea by coach) Title identity is TMDB/TVDB/MBID plus kind; folder names are a rendered view of identity, not the identity
- (idea by coach) A desired-state document is the only user-facing config; every flag in apply-stack.py becomes a typed field
- (idea by coach) Connection is {host, port, user, auth}; SeedIt4Me port 2097 is an instance, not architecture
- (idea by coach) Talk to the seedbox only through an SSH capability: exec, rsync/sftp, optional local-forward — never assume a public WebUI
- (idea by coach) Port-forward is a debug lens (mediaops ui sonarr) that lasts for a session, not how daemons talk
- (idea by coach) Install is a Provider trait: SwizzinBox, DockerCompose, AlreadyThere; v1 ships Swizzin plus AlreadyThere only
- (idea by coach) Version pins are first-class (Lidarr 2.14.5 / glibc) so upgrade is a conscious transaction, never a panel click
- (idea by coach) EdgeInvariant: bind 127.0.0.1 + url_base + Host $host + Forms auth; reconcile fails if any of those drift
- (idea by coach) PathSchema is a versioned grammar that compiles to PathBufs; AGENTS.md is generated from it so docs cannot lie
- (idea by coach) Plan is a first-class artifact with actions: Copy, Skip, Review, Unmonitor, DeleteRemote, Encode, Reclaim
- (idea by coach) Copy is always remote to staging .partial, verify, then atomic install into a schema path
- (idea by coach) Never walk torrent save paths; torrent files are not archive files
- (idea by coach) After Copy of usenet complete: DeleteRemote; after Copy of a library hardlink: leave the torrent
- (idea by coach) Holds are a typed queue with Approve, Reject, Research — not a folder Jellyfin might ingest
- (idea by coach) EncodePolicy(codec, depth, container, hdr) yields Keep, NvencH264, or Refuse; HDR/DV refuse-to-encode is a law
- (idea by coach) ResourceBudget lives in config: max_copy_gib, min_free_gib, max_nvenc, lock — no magic numbers in source
- (idea by coach) ReclaimPolicy is constraints (private, seeding, imported) times an objective (free space percent), replacing leftover timers
- (idea by coach) Discover API keys from remote config.xml and sabnzbd.ini at runtime; never store the masked ********
- (idea by coach) Indexers and download clients are sets; adding a duplicate is a conflict, not an append
- (idea by coach) GrabPolicy is data (delay, quality, indexer priority, client priority) applied via *arr API, not clicks
- (idea by coach) BootstrapLocal creates schema dirs, watermarks, systemd user timer OnUnitInactiveSec, ffmpeg/NVENC probe, and refuses if disk is below watermark
- (idea by coach) AgentTask is {prompt_template, inputs, output_schema, timeout, cwd sandbox, binary} — an LLM is a subprocess, not a brain in-process
- (idea by coach) Agents get read-only probes by default (ffprobe, ls, *arr GET); Apply is an explicit grant
- (idea by coach) CLI is the API; TUI and completions are skins; every capability is a clap subcommand
- (idea by coach) Crate graph: mediaops-core, mediaops-ssh, mediaops-arr, mediaops-sync, mediaops-encode, mediaops-agent, mediaops-cli
- (idea by coach) Test with cassette fixtures of *arr JSON and directory trees; never require the live box for unit tests
- (idea by coach) Jobs have state machines; timers only start make-progress-on-ready-jobs — no overlapping hope-cron
- (idea by coach) Bulk copy uses its own SSH TCP stream and never ControlMaster multiplexing
- (idea by coach) Crash safety: *.partial is sacred; empty-dir prune never deletes partials
- (idea by coach) The library of record can run with grabber=None: sync a folder, name it, encode it — *arr is optional
- (idea by coach) Idempotent apply: running apply twice is a no-op if Edge, Grab, and Paths already match
- (idea by coach) Local sqlite maps TitleId to path to inode/hash so identity survives renames
- (idea by coach) Read ~/.ssh/config Host seedbox instead of inventing another host alias format
- (idea by coach) Custom formats (Prefer H.264, HEVC last, 10-bit last, AV1 last) are versioned policy packs the app applies
- (idea by coach) Prowlarr app URLs must include url_base; doctor checks /prowlarr/{id}/ not /{id}/
- (idea by coach) Before remote library delete, query qBit for that path; if seeding, skip — a typed guard, not a comment
- (idea by coach) Music-first uncapped then videos under budget is a job-priority law in the planner, not a hardcode in pull()
- (idea by coach) Round-trip test: parse(render(id)) == id, so year lives in folder and file the same way
- (technique) started Reverse Brainstorming
- (idea by coach) Failure: app and SeedIt4Me panel fight over nginx every minute. Inverse: detect panel fingerprint, freeze apply unless --repair-edge, alert
- (idea by coach) Failure: commit API keys to git. Inverse: zero secrets in git; keys are remote-discovered or in a keyring
- (idea by coach) Failure: multiplex 200GiB rsync through ControlMaster. Inverse: dedicated stream per bulk copy, and lint-fail if RSYNC_RSH has ControlPath
- (idea by coach) Failure: delete qBit data after copy. Inverse: torrent delete belongs to reclaim, never to sync
- (idea by coach) Failure: copy from torrents/incomplete. Inverse: allowlist of remote roots in schema; unknown paths are errors
- (idea by coach) Failure: auto-approve every hold. Inverse: holds require a human or an agent recommendation with a confidence floor
- (idea by coach) Failure: fire 8 NVENC jobs. Inverse: hardware probe sets concurrency; this box caps at 3
- (idea by coach) Failure: click Lidarr Update. Inverse: package updates are pinned; upgrade lidarr explains glibc and can refuse
- (idea by coach) Failure: two-way sync that mirrors local deletes to the seedbox. Inverse: one-way pull; remote delete only for surplus after local proof
- (idea by coach) Failure: give the LLM ssh root. Inverse: agent runtime is a capability token (ReadFs, ProbeMedia, ArrGet, ArrPost, SshExecAllowlist)
- (idea by coach) Failure: forward *arr to 0.0.0.0 on the home LAN. Inverse: default forward is localhost-only and ephemeral
- (idea by coach) Failure: allow spaces in names because Jellyfin is smart. Inverse: schema parser is a gate; install refuses
- (idea by coach) Failure: add _incoming as a Jellyfin library. Inverse: bootstrap writes a libraries manifest that lists only movies, series, music
- (idea by coach) Failure: add NZBgeek a second time. Inverse: Prowlarr indexer apply is upsert-by-name with a set diff
- (idea by coach) Failure: paste ******** into Test. Inverse: never display secrets; Test uses the discovered key; UI says key present
- (idea by coach) Failure: transcode Avatar DV to H.264. Inverse: HDR/DV and 2160p remux are a Keep-forever class
- (idea by coach) Failure: OnCalendar=hourly overlapping runs. Inverse: oneshot plus OnUnitInactiveSec plus flock, all three
- (idea by coach) Failure: ignore the 256 GiB watermark. Inverse: preflight fail if a copy would breach watermark
- (idea by coach) Failure: one giant filename regex. Inverse: composable parsers (movie, episode, track) with explicit reject bins (needs-split, needs-year)
- (idea by coach) Failure: TUI is the only UI. Inverse: TUI is optional; cron and systemd must call the CLI
- (idea by coach) Failure: abstract Ultra.cc plus Swizzin plus QuickBox in v1. Inverse: one provider well; others are unimplemented traits with tests
- (idea by coach) Failure: hot-reload config mid-rsync. Inverse: config is snapshotted at plan start; changes wait for the next job
- (idea by coach) Failure: trust *arr file-exists as local exists. Inverse: local filesystem is source of truth; *arr is told to unmonitor
- (idea by coach) Failure: let agents change quality profiles casually. Inverse: GrabPolicy changes are explicit commands with a diff
- (idea by coach) Failure: exit 0 on lock conflict silently. Inverse: lock conflict is a distinct exit code plus a skip-with-reason log line
- (idea by coach) Failure: custom Rust TCP file protocol in v1. Inverse: shell out to rsync; maybe librsync later
- (idea by coach) Failure: identity equals path string. Inverse: identity lives in sqlite of TitleId, path, inode, hash
- (idea by coach) Failure: a wizard that only works once. Inverse: every wizard step is a reconcile of desired state
- (idea by coach) Failure: copy videos first so music never arrives. Inverse: keep music-first as a planner priority, generalize later to a wants queue
- (idea by coach) Failure: follow symlinks into torrent trees. Inverse: walk never follows symlinks off the allowlist
- (idea by coach) Failure: per-file SFTP for 80 GiB. Inverse: rsync --partial on one stream
- (idea by coach) Failure: year in the file but not the folder. Inverse: round-trip parse(render(id)) == id as a CI test
- (idea by coach) Failure: dump the entire library tree into the LLM every research call. Inverse: agents get a title dossier (ffprobe, nfo, *arr payload, neighbors), not the universe
- (idea by coach) Failure: bind *arr to star and hope. Inverse: bind address is part of EdgeInvariant
- (idea by coach) Failure: auto-upgrade 1080p to 4k remux when disk is bored. Inverse: upgrade is an explicit per-title profile class (HD vs Ultra-HD)
- (idea by coach) Failure: parse scene tags into the library name. Inverse: strip list is schema, with tests from real sins like REPACJ
- (idea by coach) Failure: run encode on the seedbox CPU. Inverse: encode is a home-GPU capability; seedbox is dumb disk plus pipe
- (idea by coach) Failure: require root on the seedbox for every command. Inverse: root only for nginx and package install; everything else is user-level API
- (idea by coach) Failure: replace rsync with rclone copyto to a second cloud. Inverse: the job is home archive, not a third copy
- (idea by coach) Failure: let doctor --repair run unattended from a public laptop. Inverse: write repairs need a local confirm flag or a pin; scheduled doctor is read-only
- (idea by coach) Failure: store desired-state only on the seedbox. Inverse: desired-state lives with the home repo; the box is disposable
- (idea by coach) Failure: chatty TUI that blocks the timer. Inverse: progress is structured log plus optional TUI attach
- (technique) started Job to Be Done
- (insight) The user is not hiring a seedbox control panel; they are hiring tonight-playable media, a quiet box, and a way to ask why when something is weird
- (idea by coach) Job tonight-on-the-TV: watch TITLE ensures monitored, grabbed, pulled, encoded if needed, then prints a Jellyfin URL
- (idea by coach) Job I-just-bought-a-disk: bootstrap /mnt/storage/videos creates schema, timer, watermarks, and generated docs
- (idea by coach) Job panel-ate-nginx: seedbox doctor plus seedbox repair edge is the one command you remember
- (idea by coach) Job why-is-disk-full: seedbox df with a reclaim preview ranked by ratio, private, age
- (idea by coach) Job why-isnt-this-in-Jellyfin: why TITLE traces grab to import to hold to pull to encode to library
- (idea by coach) Job is-this-the-theatrical-cut: agent research returns a dossier (year, edition, runtime vs ffprobe)
- (idea by coach) Job Chrome-stutters: playback TITLE classifies keep/encode against client profiles and can enqueue NVENC
- (idea by coach) Job dont-get-banned: seed-goal dashboard; reclaim never touches private-under-goal
- (idea by coach) Job I-dont-want-to-SSH-to-click: all *arr settings as seedbox apply from a file you can read in git
- (idea by coach) Job new-machine: export desired-state plus title index; import bootstraps layout even before files exist
- (idea by coach) Job split-this-season-pack: needs-split is a workflow with an agent, not a pile of files
- (idea by coach) Job I-already-have-Relayer: album identity is MBID, not folder year, so remasters do not look like missing albums
- (idea by coach) Job run-while-Im-asleep: systemd oneshot is the product; the TUI is for Saturday
- (idea by coach) Job tell-me-what-will-happen: plan is the default verb; run is plan plus apply
- (idea by coach) Job I-rejected-Homework: Reject is a first-class event — never pull this release, let *arr try another
- (idea by coach) Job I-approved-Hearts: I looked at it, put it in the library, trust me — promote from the hold queue
- (idea by coach) Job research-this-filename: agent maps a scene name to TitleId without downloading
- (idea by coach) Job debug-this-mkv: agent gets ffprobe plus mediainfo plus a Jellyfin client profile and answers Direct Play vs transcode
- (idea by coach) Job set-up-a-friend: same binary, different desired-state file — cloneable ops, not a SaaS
- (idea by coach) Job feel-safe-deleting-seedbox-media: show local hash proof before remote delete
- (idea by coach) Job dont-fill-the-spinning-disk-tonight: --max-gb with a live remaining-home-disk figure
- (idea by coach) Job use-the-Ada-GPU-for-good: encode queue is a visible, pausable job, not a surprise after sync
- (idea by coach) Job when-Im-away-dont-break: scheduled doctor is read-only; write repairs need a pin or flag
- (idea by coach) Job forget-Swizzin: success metric is days without SSHing to the panel
- (idea by coach) Job I-hired-the-agent-so-I-dont-open-12-tabs: research and debug as subcommands that shell out to claude, codex, or grok
- (idea by coach) Job make-the-junk-drawer-official: _ops and _incoming become app-managed state dirs with a schema, still not libraries
- (idea by coach) Job one-binary-on-PATH: mediaops via cargo; no Python venv, no PYTHONPATH
- (idea by coach) Job cockpit: TUI panes for disk, queue, holds, last plan, agent output
- (idea by coach) Job let-me-script-it: --json on every command so other tools can compose
- (idea by coach) Job teach-the-next-agent: generated AGENTS.md plus schema so coding agents do not invent spaces in filenames
- (idea by coach) Job music-this-week-not-more-Futurama: generalize music-first into a wants queue the planner honors
- (idea by coach) Job dont-make-me-remember-port-2097: import SSH config Host seedbox
- (idea by coach) Job if-rsync-dies-at-90-continue: resume is the job — partials plus rsync --partial
- (idea by coach) Job tell-arr-I-have-it: reconcile after a manual copy, because truth drifted
- (idea by coach) Job I-want-to-see-the-diff: apply and repair print unified diffs of ini, xml, and nginx before writing
- (idea by coach) Job pick-an-LLM-per-task: research can be a web-using agent; debug-media can be a local-file agent; the app only cares about the schema out
- (idea by coach) Job I-moved-the-library: bootstrap --relocate rewrites schema roots, systemd, and the title index paths
- (idea by coach) Job show-me-holds-on-the-phone: status --json served only on localhost, or just ssh mediaops status — no extra daemon if possible
- (technique) started Failure Analysis
- (idea by coach) Panel nginx Host rewrite: what broke is 302 to localhost after a panel install; lesson: treat panel as an untrusted writer and hash nginx app confs for drift
- (idea by coach) Turn EdgeRepair into one transaction that dry-runs a diff, then applies fix-nginx plus apply-stack as a single unit
- (idea by coach) After any box install or upgrade, automatically queue an edge check before declaring success
- (idea by coach) ControlMaster starvation: 200GiB rsync collapsed other SSH; lesson: SSH module is two clients — mux for API/exec, dedicated TCP for bulk
- (idea by coach) HEVC-MP4 Chrome dropped frames: encode scan is movies/**/*.mp4 HEVC10 only; make series-skip an explicit rule, not an accident
- (idea by coach) Encode is a reversible transaction: write .converting, replace, move original to backup-hevc-originals; never delete until replace succeeds
- (idea by coach) Playback matrix in config: client profiles (Chrome, TV, Shield) times codec yields keep, encode, or refuse
- (idea by coach) importBlocked forever: Hold is a product feature showing the *arr message, ffprobe, and approve/reject/research — never leave blocked NZBs posing as library
- (idea by coach) Lidarr glibc: a compatibility matrix per provider OS; upgrade consults it and can freeze updates in config
- (idea by coach) Masked API key Test fail: secret hygiene UX is presence-boolean, rotate command, never echo
- (idea by coach) Duplicate indexer: desired indexers as a set with priority; apply deletes extras
- (idea by coach) SEEDBOX.md vs code scan-path lie: generate docs from PathSchema; mediaops docs render writes the human markdown from the same structs as the walker
- (idea by coach) decide_item skip vs upgrade: upgrade policy is never | if-better-profile | if-user-marked-UHD; default never, matching current skip
- (idea by coach) Surplus is not skip: surplus means remote can go after local proof
- (idea by coach) Music remaster year: Relayer 1974 vs Relayer.(2013) is a documented identity rule, not a clever year compare
- (idea by coach) REPACJ leftover: scene-tag stripper has tests from real library sins (REPACJ, REPACK, PROPER)
- (idea by coach) mediaops library lint finds names that violate schema, including leftover scene tags and Season 1 folders
- (idea by coach) Partial left in from-seedbox: crash recovery command sync resume lists .partial and continues; GC never deletes them
- (idea by coach) Lock: machine-global flock with pid, started_at, and command in the lockfile; status shows who holds it
- (idea by coach) Encode 8 concurrent: NVENC semaphore from a GPU probe, never a hardcoded 8
- (idea by coach) _incoming as library: doctor warns if Jellyfin libraries include _incoming or _ops
- (idea by coach) Timer semantics: OnUnitInactiveSec is the only v1 scheduler adapter; wrap systemd-user, do not invent cron math
- (idea by coach) Hardlink seeding delete: typed guard querying qBit before any remote library unlink
- (idea by coach) Workspace /home/coto/work/dev/media-sync is empty while the live app lives under ~/videos/_incoming: the new crate is the repo of record, and the live tree becomes a deploy target
- (idea by coach) Quality profile custom-format scores drifting after panel: treat CF packs as desired state and re-PUT them on apply
- (idea by coach) SABnzbd category mismatch: categories tv/movies/music are schema, asserted on SAB and on each *arr client
- (idea by coach) qBit DHT/PeX/LSD left on: privacy and tracker policy is an invariant checked by doctor
- (idea by coach) Daily seedbox-reclaim.timer leftover no-op: reclaim is either a real policy with a dry-run or it is removed, never a silent timer
- (idea by coach) Needs-review music sitting (Daft Punk Homework, The Who, Yes): the hold queue needs a TUI/CLI inbox with age, size, and *arr reason or it becomes a junk drawer again
- (idea by coach) House.of.the.Dragon S02E07 REPACJ: lint-on-install would have stripped or rejected; install is the quality gate, not a later mop
- (idea by coach) encode_h264.py does not convert H.264 10-bit even though AGENTS.md says to: policy packs must drive the encoder or docs will keep lying
- (idea by coach) Failure of scripts as a genre: they encode tribal knowledge in comments; the app encodes it in types, tests, and generated docs
- (idea by coach) Port-forward as a product failure mode: if the home app talks to forwarded ports, a laptop sleep kills apply; therefore apply must be SSH-exec on the box or HTTP through an SSH tunnel the process owns
- (idea by coach) LLM agent failure: unbounded context of the whole library; fix is the dossier pattern plus a max-bytes budget per task
- (idea by coach) LLM agent failure: it invents a rename with spaces; fix is the agent may propose, the schema parser is the only writer
- (idea by coach) LLM agent failure: it calls *arr POST because the prompt was vague; fix is capability tokens and a dry-run default
- (idea by coach) Single-binary failure: wrapping rsync, ffmpeg, ssh, systemd, jellyfin-ffmpeg means the app is an orchestrator — declare those as external runtime deps with probes, do not vendor them
- (insight) The app is a reconciler of desired state across two machines, not a seedbox installer with a sync script taped on
- (insight) Port-forwarding is a debug affordance; the real remote API is SSH plus localhost HTTP on the box, owned by the process so a laptop sleep cannot kill apply
- (insight) Local filesystem is the library of record; *arr is a grabber you tell the truth to; identity is TitleId not path
- (insight) PathSchema is the single writer: generated docs, install gate, agent proposals, and Jellyfin contract all compile from one grammar so SEEDBOX.md cannot lie and REPACJ cannot land
- (insight) Every tribal script is a transaction type in a Plan: apply-stack, fix-nginx, encode, reclaim, review, reconcile — plan is the default verb, run is plan plus apply
- (insight) Agents are typed tools with capability tokens and a title dossier, not a chat sidebar with root; they may propose, the schema parser is the only writer
- (insight) The jobs that actually matter: tonight-playable, quiet box (days without the panel), why-isnt-this-here, and resume-not-restart; install/remove tools is in service of those jobs, not the product
- (insight) Failure history (panel Host, ControlMaster, HEVC-MP4, holds, glibc, masked keys, duplicate indexers, docs vs code) is the test suite, not folklore
- (decision) v1 provider is this SeedIt4Me/Swizzin box plus this home disk; multi-panel abstractions wait behind a trait
- (decision) CLI-first crate workspace (mediaops-*) with desired-state file, doctor/repair, sync planner preserving current laws, schema-generated docs, agent subprocess, and ui as localhost-only ephemeral forward
- (decision) Preserve current sync laws: one-way pull, never torrent roots, music-first, usenet delete after copy, torrents stay while seeding, holds as inbox, OnUnitInactiveSec plus flock, rsync not a custom protocol
- (event) session complete
- (direction by user) Seedboxes ship rclone and other sync tools; prefer the one with more threads or multi-connection so bandwidth is actually used
- (direction by user) Document the full idea, but implement a very detailed modular system: abstractions and full API clients (*arr stack, rclone, ssh wrappers), not thin scripts
- (direction by user) Must bootstrap a new seedbox and a new local media directory; Jellyfin or Plex configuration is out of scope
- (direction by user) Generate all markdown artifacts for the next BMad session; decide product brief vs spec
- (override) Reverse the v1 rsync-or-religion decision: live pull already uses rclone copyto over FTP with 8 multi-thread streams because seedbox proftpd PassivePorts is 10 slots; rsync remains a backend not the default
- (insight) Transfer is a backend trait (rclone-ftp, rclone-sftp, rsync-ssh); bottleneck may be PASV slots or sshd MaxSessions not rclone flag count; probe at bootstrap and persist the winner
- (insight) Many small flacs want --transfers>1; huge mkv wants --multi-thread-streams on one file; the planner should pick per-item not one global flag
- (insight) Implementation philosophy is thick libraries thin CLI: complete Servarr API clients, typed rclone wrapper, SSH exec vs bulk split, so the product never hits a missing endpoint
- (decision) Bootstrap is two first-class commands: seedbox (connection, provider, packages, desired-state, paths) and local media dir (schema, watermarks, timer, lock, rclone remote, state). Not a media-server installer
- (decision) Non-goal: configuring Jellyfin or Plex libraries, users, or playback clients
- (decision) Next BMad session is spec not product-brief: the idea is already decided and the operator wants a machine contract; brief would rediscover known jobs. Architecture after spec owns crate graph and full API HOW
- (direction by user) Do not use SSH plus localhost HTTP as the formal API. Want a managed way to forward traffic; OK installing a service, agent, or CLI on the seedbox
- (direction by user) rclone vs rsync (FTP or other) is for research to pick the best tool even if not currently in use; same for *arr, SABnzbd, and other seedbox tools
- (insight) Split planes: control is a versioned RPC on mediaopsd; data is HTTP Range over the same overlay. *arr/SAB/qBit stay localhost-only and are never the remote API
- (insight) SSH -L is an implementation hack: no identity, no reconnect policy, laptop sleep kills apply. Tailscale Serve (or Headscale) is a managed forwarder of localhost services onto a tailnet; rathole is the no-account Rust fallback
- (insight) rclone multi-thread is built for HTTP Range and object stores. FTP on this box is capped by proftpd PassivePorts=10. SFTP is CPU-heavy and incomplete for multi-thread. rsync is one TCP stream and leaves WAN BDP idle (Geerling ~4x gap vs parallel rclone on large files)
- (insight) Inside an already-encrypted overlay, serving allowlisted local files over HTTP Range from mediaopsd (or rclone serve http) plus home rclone copy --multi-thread-streams is the throughput path. Double-crypto SFTP over WireGuard is waste
- (decision) Formal API is mediaopsd RPC (Connect-RPC or gRPC, mTLS). Home CLI talks to local mediaopsd; seedbox mediaopsd is the only process that talks to *arr on 127.0.0.1. SSH is bootstrap/install only
- (decision) Managed overlay default: Tailscale (userspace on seedbox if no TUN) plus tailscale serve of mediaopsd. Fallback: rathole (Rust). Not Cloudflare Tunnel for bulk. Not SSH reverse-forward as the product
- (decision) Data plane: HTTP Range. Seedbox serves allowlisted paths; home rclone copyto with multi-thread-streams and separate TCP connections (disable keep-alive if HTTP/2 multiplexing caps throughput). Probe stream count at bootstrap. Not FTP, not rsync default, not SFTP default
- (decision) Keep Sonarr Radarr Prowlarr qBittorrent SABnzbd Lidarr-pinned. Drop Jackett. Autobrr optional not core. Absorb Recyclarr/TRaSH as GrabPolicy via our API client, do not require the recyclarr binary. Bazarr optional. Do not switch to NZBGet or rtorrent
- (decision by user) Overlay default is rathole. Tailscale and WireGuard are optional (recommended, not required)
- (override by user) rathole means the idea not the app: do not install rathole. mediaopsd binds the server. Auth is gRPC with mTLS, self-signed certs generated at bootstrap. Token/key allowed; mTLS is the preference
