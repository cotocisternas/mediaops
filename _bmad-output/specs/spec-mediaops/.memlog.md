---
topic: mediaops — CLI + seedbox daemon reconciling desired state across a rented seedbox and a home archive disk
updated: 2026-08-29T00:51
---

- (direction) Create spec slug mediaops from three brainstorm files in last-wins order; extract directly; no coaching walkthrough; kernel lean; overflow in named companions; offer story breakdown after SPEC.md; next fresh window is bmad-architecture.
- (direction) Locked, do not re-open: mediaopsd gRPC+mTLS with bootstrap-minted self-signed CA+server+client certs; token optional 2FA, mTLS preferred; in-process overlay (rathole the idea not the app); mediaopsd binds; optional reverse-connect if bind side NATed; Tailscale/WG optional recommended underlay; SSH bootstrap only not API not ssh -L; data plane parallel Range RPCs on same gRPC/mTLS not FTP/rsync/SFTP/rclone-as-pipe; bootstrap both seedbox and local media dir; Jellyfin/Plex config out; keep Sonarr/Radarr/Prowlarr/SABnzbd/qBittorrent/Lidarr-pinned; Jackett out; Recyclarr as GrabPolicy data not required binary; v1 this SeedIt4Me/Swizzin box + this home disk; local FS library of record; *arr optional grabber; one-way pull; never torrent save paths.
- (note) Persistent facts file project-context.md is absent; no project-context loaded.
- (decision) Interactive extract: input is rich; distill WHAT not HOW; later source lines override earlier; user locks override all three files.
- (decision) Brainstorm .memlog.md is not a peer source (the three files absorb it). No brainstorm memlog present on disk at distill time.
- (decision) Library bootstrap does not create an rclone remote; home talks Range RPCs to seedbox mediaopsd (modules.md last-wins over product-idea/intent rclone-remote line). rclone/rsync/rathole are not required runtime deps for overlay pull.
- (decision) CLI verbs: seedbox bootstrap and library bootstrap (modules.md last-wins over product-idea local bootstrap).
- (decision) Home also runs mediaopsd on a unix socket so the CLI never holds overlay credentials per invocation (modules.md last-wins). Non-goal remains no extra public status daemon.
- (decision) Same-file leftover 'typed rclone flags' in modules philosophy is superseded by later modules sections: rclone is not the pipe.
- (capability) CAP-1 Tonight-playable: watch a title to monitored+grabbed+pulled+encoded-if-needed playable schema file on home disk without breaching budget; upgrade default never.
- (capability) CAP-2 Quiet box: keep Edge/Grab/Paths correct from git-readable desired-state; second apply no-op; diffs before write; days without panel SSH.
- (capability) CAP-3 Why-trace: grab→import→hold→pull→encode→library with local FS as truth; unmonitor if *arr is wrong; df + reclaim preview for disk-full.
- (capability) CAP-4 Resume-not-restart: kill copy at 90%; resume continues from sacred .partial; GC never deletes partials.
- (capability) CAP-5 Bootstrap seedbox: new Swizzin or AlreadyThere box from desired-state including mediaopsd mTLS, pins, edge, grab policy, secret discovery, transfer probe.
- (capability) CAP-6 Bootstrap local library: new home media dir — schema roots, app-managed _ops/_incoming (never libraries), watermarks, lock, systemd oneshot+OnUnitInactiveSec+flock, title-index, NVENC probe; no Jellyfin/Plex.
- (capability) CAP-7 Plan and run: plan is default verb with Copy/Skip/Review/Unmonitor/DeleteRemote/Encode/Reclaim; run=plan+apply; snapshot at plan start; lock conflict distinct exit not silent 0.
- (capability) CAP-8 Holds inbox: Approve/Reject/Research; reject=never this release; no auto-approve; agent recommend needs confidence floor; blocked NZBs are not library.
- (capability) CAP-9 Reclaim: real policy with dry-run or it does not exist; local hash proof; qBit seeding skip; private-under-goal untouchable; torrent delete is reclaim not sync.
- (capability) CAP-10 Home encode: EncodePolicy+playback matrix on home GPU; HEVC-MP4 movies encode; series-skip explicit; HDR/DV and 2160p remux Keep-forever; reversible .converting; seedbox never encodes.
- (capability) CAP-11 Agent research and debug: subprocess LLM with dossier, capability tokens, schema-out, propose-only writes; PathSchema is the only writer; no root SSH.
- (capability) CAP-12 Doctor and edge repair: panel is untrusted writer; scheduled doctor read-only; repair one transaction needing confirm/pin; EdgeInvariant drift fails reconcile; edge check after install/upgrade.
- (constraint) Formal API is mediaopsd gRPC+mTLS; bootstrap-minted self-signed CA+server+client certs never git; token optional 2FA; mTLS preferred; home CLI talks only to local mediaopsd; only seedbox mediaopsd speaks grabber HTTP on 127.0.0.1.
- (constraint) Overlay in-process: mediaopsd binds; optional reverse-connect same binary if bind side NATed; Tailscale/WG optional recommended underlay; not rathole/frp/cloudflared apps; not Cloudflare Tunnel for bulk; not ssh -L/-R as product.
- (constraint) SSH is bootstrap only (install unit, mint/place certs, Swizzin root for nginx+packages). After enroll, doctor/apply/copy do not use SSH. Never bulk copy over SSH.
- (constraint) Data plane: parallel Range RPCs on same gRPC/mTLS; allowlisted library/usenet-complete paths only; not FTP/rsync/SFTP/rclone-as-pipe; probe+persist N; many files vs many ranges; do not collapse onto one TCP.
- (constraint) One-way pull; remote delete only surplus after local hash proof; never two-way; never third cloud; Push is not a product surface.
- (constraint) Never torrent save paths or torrents/incomplete; PathSchema remote-root allowlist; unknown paths error; never follow symlinks off allowlist.
- (constraint) Copy path always remote → staging .partial → verify → atomic schema install; .partial sacred; resume lists and continues; GC never deletes partials.
- (constraint) Usenet DeleteRemote after Copy; torrents stay while seeding; torrent delete is reclaim; qBit path query before unlink, skip if seeding.
- (constraint) Skip ≠ surplus: skip = do not copy; surplus = remote may go after local proof.
- (constraint) Local FS is library of record; *arr optional (grabber=None valid); never trust *arr file-exists as local-exists.
- (constraint) Identity is TitleId (kind + TMDB/TVDB/MBID) not path; sqlite TitleId→path→inode/hash; music remasters by MBID not folder year.
- (constraint) PathSchema is single writer for names, install gate, lint, agent proposals, generated docs; parse(render(id))==id; scene-tag strip REPACJ/REPACK/PROPER; spaces refuse; reject bins include needs-split and needs-year.
- (constraint) Holds are inbox not library; _ops and _incoming are app-managed never libraries.
- (constraint) EdgeInvariant = bind 127.0.0.1 + url_base + Host $host + Forms auth; panel untrusted; hash nginx app confs; Prowlarr app URLs include url_base; qBit DHT/PeX/LSD off is doctor invariant; SAB categories tv/movies/music on SAB and each *arr client; warn if an existing media server libraries _incoming/_ops; root only for nginx+packages; mediaopsd user-level.
- (constraint) Encode on home GPU only; EncodePolicy(codec,depth,container,hdr)→Keep|NvencH264|Refuse; playback matrix is config not a media-server API; HDR/DV and 2160p remux Keep-forever; NVENC from probe (this box cap 3); upgrade class never|if-better-profile|if-user-marked-UHD default never.
- (constraint) ResourceBudget in config (max_copy_gib, min_free_gib, max_nvenc, lock); no magic numbers; preflight fail if copy would breach watermark; 256 GiB is a config value.
- (constraint) Scheduler: systemd-user oneshot + OnUnitInactiveSec + flock all three; lockfile pid/started_at/command; lock conflict distinct exit never silent 0; jobs are state machines; timer only advances ready jobs; no OnCalendar overlapping hope-cron; no hot-reload mid-copy.
- (constraint) Indexers/clients are sets (upsert-by-name; duplicate=conflict; apply deletes extras); CF packs desired-state re-PUT on apply; version pins first-class (Lidarr 2.14.5/glibc); upgrade is a transaction that can refuse; GrabPolicy changes are explicit commands with diffs.
- (constraint) v1 grabber stack: Sonarr, Radarr, Prowlarr, SABnzbd, qBittorrent, Lidarr version-pinned; Jackett out; Recyclarr binary not required; Autobrr/Bazarr optional not core; *arr HTTP never leaves localhost. Details in grabber-inventory.md.
- (constraint) Zero secrets in git; discover keys from remote config.xml/sabnzbd.ini; never store or echo ********; UI is presence boolean; Test uses discovered key.
- (constraint) Agents are subprocess CLI LLMs not in-process; tokens ReadFs/ProbeMedia/ArrGet/ArrPost/SshExecAllowlist; default read-only; never ssh root; no casual *arr POST.
- (constraint) CLI is the API: clap subcommand per capability; --json on every command; TUI optional skin timers must not require; import ~/.ssh/config Host seedbox; human WebUI ui <app> is session-scoped localhost overlay not apply path; status --json is localhost or ssh mediaops status — no extra public daemon.
- (constraint) v1 is this SeedIt4Me/Swizzin box + this home disk; Provider trait may exist; v1 ships SwizzinBox + AlreadyThere only; Ultra.cc/QuickBox unimplemented trait+tests not ports; SeedIt4Me 2097 is instance not architecture; Connection is {host,port,user,auth} plus gRPC bind and mTLS identity.
- (constraint) Thick libraries, thin CLI; grabber HTTP clients only inside mediaopsd; home CLI never speaks Sonarr HTTP; architecture may shuffle impl crates but must not put grabber HTTP in the CLI or FTP in transfer. Module responsibilities in module-map.md.
- (constraint) Planner honors music-first then videos under ResourceBudget (later wants queue); not a hardcode inside pull.
- (constraint) This workspace is the repo of record; live ~/videos is a deploy target.
- (constraint) Unit tests never require the live box; failure history is the test suite (failure-history-tests.md).
- (constraint) Runtime deps probed not vendored: mediaopsd, ssh (bootstrap), ffmpeg/jellyfin-ffmpeg, ffprobe, systemd-user, CLI LLM binaries; optional tailscale/wireguard; rathole/rclone/rsync not required for overlay pull.
- (decision) Non-goals: Jellyfin/Plex configuration; working Ultra.cc/QuickBox providers in v1; custom file-transfer protocol; FTP/rsync/SFTP/rclone-as-pipe; rathole/frp/cloudflared apps; Cloudflare Tunnel for bulk; two-way sync; third-cloud archive; Push; SSH+localhost HTTP as API; ssh -L as product; public WebUI; LAN bind of *arr; extra public status daemon; SaaS; in-process LLM; dump whole library into context; agents with root SSH; TUI-only; Jackett; requiring Recyclarr binary; auto-approve holds; auto-upgrade HD→UHD; encode on seedbox; adding _incoming/_ops as a media-server library; panel-click upgrades; unpinned Lidarr updates; OnCalendar overlapping cron; hot-reload mid-copy; identity=path; media-server installer/plugin; vendoring ffmpeg/ssh.
- (decision) Success signal: new Swizzin box + new home disk bootstrapped from desired-state so mediaopsd answers gRPC/mTLS; a title traces grab→library with local FS as truth; copy killed at 90% resumes from .partial; second apply is no-op when Edge/Grab/Paths match; parallel Range RPCs beat live FTP-PASV ~30 MiB/s; one binary on PATH no Python venv; scheduled doctor does not write; lock conflict is a distinct exit; holds inbox does not rot; HEVC-MP4 movies encode under probe cap while HDR/DV remuxes are kept.
- (decision) Spec-authored companions: module-map.md (intended module responsibilities, must-nots, CLI surface, tribal replacements), grabber-inventory.md (stack, API completeness, sets, secrets, optionals), bootstrap-surfaces.md (seedbox vs library bootstrap matrix, relocate, new-machine), failure-history-tests.md (cassettes, fixtures, named failures as suite). Kernel cites them. Architecture owns crate-graph HOW.
- (assumption) Home mediaopsd unix socket is required (modules last-wins); the 'no extra daemon' non-goal means no extra public status daemon.
- (assumption) Instance numbers NVENC cap 3, SeedIt4Me port 2097, 256 GiB watermark, Lidarr 2.14.5 are config/matrix rows not architecture constants.
- (assumption) Autobrr/Bazarr remain optional non-core; v1 need not ship them.
- (assumption) Emergency unadvertised rsync-ssh may exist; it is not a product surface.
- (assumption) Human WebUI ui <app> is in v1 as session-scoped localhost overlay (product-idea v1-in plus modules CLI table).
- (question) Which content-hash algorithm is local-proof and .partial verify?
- (question) How is the agent hold-recommend confidence floor expressed, and what is the v1 number if any?
- (question) At rest, where do CA/server/client certs live besides never-git — keyring, files next to desired-state, or both?
- (note) Wrapper-only dropped: process next-steps (spec then architecture), 'this document is the source' meta, long research rationale for rejected pipes (picks kept), hold folklore names as examples, Avatar DV as anecdote (Keep-forever law kept), Geerling rclone citation (lesson kept as parallel ranges).
- (decision) Preservation fill: when encode is chosen the target is H.264 8-bit; year lives in folder and file the same way; v1 playback profiles are Chrome, TV, Shield; status shows who holds the lock.
- (event) Derived SPEC.md plus companions module-map.md, grabber-inventory.md, bootstrap-surfaces.md, failure-history-tests.md from this memlog and the three cited sources.
- (event) Self-validate pass 1 coherence: pass. Spec Law 1–6 and 8: 12 capabilities each with intent+success; intents are operator jobs (WHAT); constraints bend design; non-goals explicit; success signal is demonstrable; CAP-1..CAP-12 unique and stable; kernel cites companions for catalogs.
- (event) Self-validate pass 2 preservation: pass after the H.264-8-bit / year-round-trip / playback-profiles / lock-holder fill-ins. Last-wins conflicts (rclone remote, local vs library bootstrap, home mediaopsd vs extra-daemon slogan) logged as decisions. Wrapper-only drops already logged.
- (direction) Party-mode picks (Coto, 2026-08-28) last-win into this spec. Do not re-open locked API/overlay/SSH/Range laws.
- (capability) CAP-1 updated: watch is enqueue. Command success = wanted + monitored if grabber on (wanted only if grabber=None). Playable is the timer/run job, including encode. No attached console. Peek via why/status. Upgrade default never.
- (capability) CAP-8 updated: human inbox only. Auto-approve impossible. No agent-approve path in v1 so no confidence floor. Research as a hold verb may later call CAP-11; first demo has no LLM.
- (decision) BLAKE3 is the only digest: per-range in the .partial map and whole-file at schema install. Reclaim local-proof uses only the install digest. No xxHash. No MD5.
- (decision) mTLS material lives as gitignored files next to desired-state. Desired-state stores fingerprints + paths, never PEMs. Doctor refuses if cert PEMs are inside a git work tree. new-machine copies the tls dir. Re-mint is the disaster path.
- (decision) v1 this box: seedbox binds gRPC/mTLS. Reverse-connect and Tailscale/WG are designed, unused by default, not required to demo the first Range RPC.
- (decision) Home mediaopsd unix socket is required. CLI talks only to that socket and never contains a seedbox address. Assumption promoted to constraint.
- (decision) Transfer verify is per-range BLAKE3 on the wire plus whole-file BLAKE3 at install. Size/mtime is not proof.
- (decision) First demo on this box includes the pipe (plan, Range pull, .partial resume, schema install) AND encode (HEVC-MP4 movie under NVENC). grabber=None is a valid demo path. Unit tests still do not require the live box or a GPU; GPU is this-box demo.
- (decision) Deferred from first demo (kernel may keep the capability): TUI, ui <app>, CAP-11 LLM agents, bearer token (v1 is mTLS only), generalized wants queue beyond music-first + per-title watch-want.
- (decision) No emergency rsync-ssh path. Hidden pipe would get used. SSH bulk copy remains a test failure. Supersedes 'unadvertised rsync may exist'.
- (decision) No Autobrr or Bazarr, not even stubs. Supersedes optional-non-core-may-exist.
- (decision) Closed open questions: hash=BLAKE3; certs=gitignored files + fingerprints; agent floor=none because no agent-approve in v1.
- (decision) New companion increments.md: first demo vs designed-unused vs deferred vs forbidden.
- (event) Re-derived SPEC.md and companions (including new increments.md) from party last-wins.
- (event) Self-validate pass 1 coherence: pass. CAP-1 and CAP-8 intents/success updated in place; IDs stable. Open questions closed. Constraints still decision-bending. Non-goals include rsync-ssh, Autobrr, Bazarr.
- (event) Self-validate pass 2 preservation: pass. Party picks landed (BLAKE3 both layers, gitignored TLS files, bind default, home unix socket, watch enqueue vs timer playable, encode in first demo, deferrals, forbidden rsync/Autobrr/Bazarr). Locked API/overlay/SSH/Range laws unchanged.
- (decision) Adopted companion: ../../planning-artifacts/architecture/architecture-mediaops-2026-08-29/ARCHITECTURE-SPINE.md (status final, 2026-08-29) — the architecture spine owning the crate-graph HOW the kernel delegates (22 ADs with stable AD-N IDs, consistency conventions, verified stack, structural seed, CAP-1..12 map). bmad-architecture owns and edits it; the spec references, never edits. Downstream cites AD IDs directly.
- (decision) Kernel touch on adopt: the thick-libraries/thin-CLI constraint line now also cites ARCHITECTURE-SPINE.md for crate-graph HOW alongside module-map.md (module map = intended responsibilities; spine = binding ADs and CI-enforced dependency edges). No capability, constraint semantics, or ID changed; no content moved.
- (event) Re-derived SPEC.md: companions gained the adopted ARCHITECTURE-SPINE.md; single constraint cite added. CAP IDs untouched.
- (event) Self-validate pass 1 coherence: pass — adoption-only update; kernel fields, CAP-1..12, constraints, non-goals, success signal unchanged; companions list remains discoverable and content-typed.
- (event) Self-validate pass 2 preservation: pass — no source claims moved or dropped; the spine is referenced, not absorbed, so no wrapper-only drops. Architecture memlog remains the spine rationale of record (not listed in frontmatter per process-metadata rule).
- (decision) Story breakdown run (interactive, Coto): scope = first demo per increments.md, grabber=None throughout — arr crate and grabber RPCs excluded from this story set. 13 stories in execution order, each citing spine AD IDs. Checkpoint posture: spec_checkpoint on 4/7/9/13; done_checkpoint on 7 and 13. Global invoke_dev_with: offline tests only (AD-20), obey arch-tests; stories 7 and 13 add live-box caution.
- (decision) Stories 1-7: 1 workspace scaffold + arch-tests dependency law (AD-1/2/17/18/19); 2 PathSchema/TitleId/walker + schema CI (AD-13); 3 DesiredState/Plan/jobs/store (AD-8/9/10); 4 proto wire contract incl ErrorDetail + ControlPort (AD-3) [spec_checkpoint]; 5 net TLS mint + channel pool + endpoint_fingerprint (AD-14); 6 mediaopsd seedbox role: mTLS serve, Transfer over walker, df + version handshake (AD-5/22); 7 seedbox bootstrap over ssh: musl build, unit, certs, config-home refusals, probe-persist N (AD-7/16/22) [spec_checkpoint, done_checkpoint].
- (decision) Stories 8-13: 8 home gateway: UDS re-serve, byte-for-byte proxy, ConfigurePool + 1:1 pinning + ResourceExhausted (AD-4/12); 9 transfer pull/verify/resume: scheduler, .partial + sidecar, install() handoff (AD-11/12) [spec_checkpoint]; 10 library bootstrap + scheduler: schema dirs, _ops/_incoming, watermark refuse, systemd units + lock classes, NVENC probe (AD-6/7); 11 sync plan/apply + CLI verbs plan/run/status/watch/why --json (AD-9/10); 12 encode: policy, NVENC cap, .converting via replace(), pausable queue, refuse classes (AD-13/16); 13 first demo on this box, live integration + runbook [spec_checkpoint, done_checkpoint].
- (event) stories.yaml rendered and checked against stories-schema rules: 13 entries, ids 1..13 quoted/unique/prefix-free, no status field, required fields present — pass.
